\documentclass[tikz, border=10pt]{standalone}
\usetikzlibrary{shapes, arrows.meta, positioning, calc}

\begin{document}
	\begin{tikzpicture}[
		block/.style = {draw, fill=blue!10, rectangle, minimum height=3.5em, minimum width=6.5em, align=center, rounded corners=2pt},
		sum/.style = {draw, fill=white, circle, inner sep=2pt},
		input/.style = {coordinate},
		output/.style = {coordinate},
		node distance=2cm,
		>={Stealth[scale=1.2]}
		]
		
		% --- CAPA CENTRAL (EJE PRINCIPAL) ---
		\node [sum] (sum_accel) {\Large +};
		
		% Bloques de control
		\node [block, left=2.5cm of sum_accel, yshift=1.6cm] (ctrl_pos) {Control \\ Posición ($K_p, K_d$)};
		\node [block, left=2.5cm of sum_accel, yshift=-1.6cm] (ctrl_en) {Control \\ Energía ($K_e$)};
		
		% Entradas
		\node [input, left=1.2cm of ctrl_pos] (ref_x) {};
		\node [input, left=1.2cm of ctrl_en] (ref_e) {};
		
		% --- PROCESAMIENTO Y PLANTA ---
		\node [block, right=1.5cm of sum_accel, fill=green!5] (pfl) {\textbf{PFL}};
		\node [block, right=1.8cm of pfl, fill=orange!5, minimum height=5em, minimum width=8.5em] (planta) {\textbf{Sistema Real}};
		\node [output, right=2.5cm of planta] (salida) {};
		
		% --- CONEXIONES PRINCIPALES ---
		\draw [->] (ref_x) -- node[above, pos=0.1] {$x_{ref}$} (ctrl_pos);
		\draw [->] (ref_e) -- node[above, pos=0.1] {$E_{ref}$} (ctrl_en);
		
		\draw [->] (ctrl_pos.east) -| node[right, pos=0.9] {\small $-$} (sum_accel);
		\draw [->] (ctrl_en.east) -| node[right, pos=0.9] {\small $+$} (sum_accel);
		
		\draw [->] (sum_accel) -- node[above] {$\ddot{x}_{des}$} (pfl);
		\draw [->] (pfl) -- node[above] {$u$} (planta);
		\draw [->] (planta) -- (salida) node[above left, xshift=1cm] {$[x, \theta, \dot{x}, \dot{\theta}]^T$};
		
		% --- RETROALIMENTACIÓN (Bucle cerrado optimizado) ---
		
		% 1. Lazo Interno: PFL (Estado completo)
		\coordinate (branch_interno) at ($(planta.east) + (0.6,0)$);
		\draw [->] (branch_interno) -- ++(0,-2.2) -| node[pos=0.7, right] {$[x, \theta, \dot{x}, \dot{\theta}]^T$} (pfl.south);
		
		% 2. Lazo Externo: Punto de derivación único para Controles
		\coordinate (branch_externo) at ($(planta.east) + (1.4,0)$);
		
		% Hacia Posición (Arriba)
		\draw [->] (branch_externo) -- ++(0,2.8) -| node[pos=0.0, right] {$[x, \dot{x}]$} (ctrl_pos);
		
		% Hacia Energía (Abajo)
		\draw [->] (branch_externo) -- ++(0,-2.8) -| node[pos=0.0, right] {$[\theta, \dot{\theta}]$} (ctrl_en);
		
		% Punto de unión visual para el branch externo
		\fill (branch_externo) circle (1.5pt);
		\fill (branch_interno) circle (1.5pt);
		
	\end{tikzpicture}
\end{document}