%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% The Legrand Orange Book
% LaTeX Template
% Version 2.3 (8/8/17)
%
% This template has been downloaded from:
% http://www.LaTeXTemplates.com
%
% Original author:
% Mathias Legrand (legrand.mathias@gmail.com) with modifications by:
% Vel (vel@latextemplates.com)
%
% License:
% CC BY-NC-SA 3.0 (http://creativecommons.org/licenses/by-nc-sa/3.0/)
%
% Compiling this template:
% This template uses biber for its bibliography and makeindex for its index.
% When you first open the template, compile it from the command line with the 
% commands below to make sure your LaTeX distribution is configured correctly:
%
% 1) pdflatex main
% 2) makeindex main.idx -s StyleInd.ist
% 3) biber main
% 4) pdflatex main x 2
%
% After this, when you wish to update the bibliography/index use the appropriate
% command above and make sure to compile with pdflatex several times 
% afterwards to propagate your changes to the document.
%
% This template also uses a number of packages which may need to be
% updated to the newest versions for the template to compile. It is strongly
% recommended you update your LaTeX distribution if you have any
% compilation errors.
%
% Important note:
% Chapter heading images should have a 2:1 width:height ratio,
% e.g. 920px width and 460px height.
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


%----------------------------------------------------------------------------------------
%	PACKAGES AND OTHER DOCUMENT CONFIGURATIONS
%----------------------------------------------------------------------------------------

\documentclass[11pt,fleqn]{book} % Default font size and left-justified equations

%----------------------------------------------------------------------------------------

\input{structure} % Insert the commands.tex file which contains the majority of the structure behind the template

%IMPORTS MAPA MENTAL
\usepackage{tikz,times}
\usepackage{verbatim}
\usepackage{standalone}
\usepackage{placeins}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{algpseudocode}
\usepackage{subcaption}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{tcolorbox}
\tcbuselibrary{listings, skins}

\floatname{algorithm}{Algoritmo}
\usetikzlibrary{positioning, arrows, arrows.meta, shapes, shapes.geometric, calc, fit, backgrounds, mindmap, trees}
\usetikzlibrary{patterns, decorations.pathreplacing}
%\usepackage[paperwidth=25cm,paperheight=22cm,left=1cm,top=1cm]{geometry}

\usetikzlibrary{mindmap,trees,backgrounds}
 
\definecolor{color_mate}{RGB}{255,255,128}
\definecolor{color_petr}{RGB}{255,192,192}
\definecolor{color_made}{RGB}{192,255,192}
\definecolor{color_meta}{RGB}{192,192,255}
\definecolor{color_text}{RGB}{255,150,150}
\definecolor{color_plas}{RGB}{150,150,255}
% Definición de colores estilo Matlab/Moderno
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.97,0.97,0.98} % Fondo gris muy suave
\definecolor{framecolour}{rgb}{0.85,0.88,0.92} % Borde azul-grisáceo suave

% Estilo de las líneas de código
\lstdefinestyle{matlabstyle}{
	language=MATLAB,
	commentstyle=\color{codegreen},
	keywordstyle=\color{blue},
	numberstyle=\tiny\color{codegray},
	stringstyle=\color{codepurple},
	basicstyle=\ttfamily\small, % Fuente monoespaciada limpia
	breakatwhitespace=false,         
	breaklines=true,                 
	keepspaces=true,                 
	showspaces=false,                
	showstringspaces=false,
	showtabs=false,                  
	tabsize=4,
	extendedchars=true,
	literate={Ó}{{\'O}}1 {Ó}{{\'O}}1 {Í}{{\'I}}1 {Ú}{{\'U}}1 {á}{{\'a}}1 {é}{{\'e}}1 {í}{{\'i}}1 {ó}{{\'ó}}1 {ú}{{\'u}}1 {ñ}{{\~n}}1 % Corrección de tildes
}

\newtcblisting{matlabbox}[1]{
	arc=3mm,                  % Esquinas redondeadas suaves
	colback=backcolour,       % Fondo suave
	colframe=framecolour,     % Color del borde
	enhanced,
	listing only,
	listing options={style=matlabstyle},
	title=#1,                 % Título de la caja
	coltitle=black,           % Color del texto del título
	fonttitle=\small\sffamily\bfseries,
	attach boxed title to top left={yshift=-2mm, xshift=5mm}, % Ajustado sutilmente
	boxed title style={colback=framecolour, colframe=framecolour, arc=1mm, top=1mm, bottom=1mm}, % Caja de título más delgada
	% --- NUEVOS AJUSTES DE COMPACTACIÓN ---
	top=1.5mm,                % Reduce espacio interno arriba del código
	bottom=1.5mm,             % Reduce espacio interno abajo del código
	beforeafter skip=3mm      % Reduce drásticamente el espacio entre bloques sucesivos
}
\raggedbottom
\begin{document}
%----------------------------------------------------------------------------------------
%	TITLE PAGE
%----------------------------------------------------------------------------------------	
\begingroup
\thispagestyle{empty}
% Usamos TikZ para posicionamiento absoluto en la página
\begin{tikzpicture}[remember picture,overlay]
	
	% --- 1. LOGO DE LA UJI ---
	% Mantenemos la posición del logo original
	\node[anchor=north, yshift=-2.5cm] at (current page.north) {
		\includegraphics[height=5cm]{Pictures/logo_uji.png} % Ajusta la altura si es necesario
	};
	
	% --- 2. HEADER TEXT (NUEVO ESTILO) ---
	% Text nodes for the structured header
	\node[anchor=center, yshift=6cm] at (current page.center) {
		{\huge\sffamily\scshape TRABAJO DE FINAL DE GRADO}
	};
	\node[anchor=center, yshift=4cm] at (current page.center) {
		{\large\sffamily GRADO EN INTELIGENCIA ROBÓTICA}
	};
	
	% --- 3. MAIN TITLE WITH LINES --
	% Upper Line
	\draw[thick] ([xshift=-0.425\paperwidth, yshift=2cm]current page.center) -- ([xshift=0.425\paperwidth, yshift=2cm]current page.center);
	
	% Actual Title Text
	\node[anchor=center, yshift=0cm] at (current page.center) {
		\parbox{0.85\paperwidth}{
			\centering
			\fontsize{24pt}{28pt}\selectfont 
			\bfseries\sffamily
			Estudio e implementación del control de péndulo invertido sobre carro en un sistema de tiempo real
		}
	};
	% Lower Line
	\draw[thick] ([xshift=-0.425\paperwidth, yshift=-2cm]current page.center) -- ([xshift=0.425\paperwidth, yshift=-2cm]current page.center);
	
	% --- 4. STUDENT AND TUTOR INFO SPLIT (NUEVO ESTILO) ---
	% Left info (right-aligned text within the parbox)
	\node[anchor=north, xshift=-7cm, yshift=-5cm] at (current page.center) {
		\parbox{0.45\paperwidth}{
			\raggedleft % Alineado a la derecha dentro de la caja
			{\normalsize\sffamily Estudiante:}\\[5pt]
			{\Large\sffamily\bfseries Miguel Porcar Vicent}\\
		}
	};
	
	% Right info (right-aligned text within the parbox)
	\node[anchor=north, xshift=3cm, yshift=-5cm] at (current page.center) {
		\parbox{0.45\paperwidth}{
			\raggedleft % Alineado a la derecha dentro de la caja
			{\normalsize\sffamily Tutor:}\\[5pt]
			{\Large\sffamily\bfseries Ignacio Peñarrocha Alós}
		}
	};
	
	% --- 5. COURSE AND DATE (NUEVO ESTILO, DISCRETO AL FINAL) ---
	\node[anchor=south, yshift=4cm] at (current page.south) {
		\parbox{0.8\paperwidth}{
			\centering
			{\large\sffamily Fecha de presentación: Junio de 2026}\\[5pt]
			{\large\sffamily Curso académico: 2025/2026}
		}
	};
\end{tikzpicture}
\vfill
\endgroup


%----------------------------------------------------------------------------------------
%	COPYRIGHT PAGE
%----------------------------------------------------------------------------------------

\newpage
~\vfill
\thispagestyle{empty}

\noindent \textit{Agradecimientos:
\\
\\} % 

\noindent \textit{Palabras clave:
\\
\\} % 

\noindent \textit{CASTELLÓN, a XX de XXXXXXX de 2025} % Printing/edition date

\noindent {Todos los derechos reservados}\\ % License information

%----------------------------------------------------------------------------------------
%	TABLE OF CONTENTS
%----------------------------------------------------------------------------------------

\usechapterimagefalse % If you don't want to include a chapter image, use this to toggle images off - it can be enabled later with \usechapterimagetrue

%\chapterimage{chapter_head_1.pdf} % Table of contents heading image

\pagestyle{empty} % No headers

\renewcommand{\contentsname}{ÍNDICE GENERAL}
\tableofcontents % Print the table of contents itself

\cleardoublepage % Forces the first chapter to start on an odd page so it's on the right

\pagestyle{fancy} % Print headers again

\chapter{Introducción}

El objeto de este proyecto es el estudio y control de un péndulo invertido sobre un carro móvil. Aunque a primera vista pueda parecer un sistema mecánico simple, en el ámbito de la \textbf{Robótica} este dispositivo constituye uno de los problemas fundamentales de control y equilibrio dinámico. 

La relevancia de este sistema reside en su transferencia directa a tecnologías críticas de la robótica moderna. El reto de mantener una barra inestable en equilibrio vertical es, en esencia, el mismo principio físico que permite a un \textbf{robot humanoide} caminar sin desplomarse, a un cohete mantenerse estable durante el despegue o a vehículos de transporte personal (como los \textit{Segways}) desplazarse de forma autónoma. Controlar este sistema implica dominar la capacidad de un robot para reaccionar ante la gravedad y las perturbaciones externas en milisegundos.

En ingeniería, este reto se utiliza para evaluar la eficacia de un sistema de control. El objetivo es que un microcontrolador (cerebro) interprete la inclinación de la barra mediante sensores (ojos) y actúe sobre un motor (músculos) con una velocidad y precisión tales que desafíen la inestabilidad natural del sistema. 

Finalmente, en el marco de la robótica, este proyecto se sitúa en la denominada \textbf{capa reactiva}. Mientras que gran parte de la robótica actual se enfoca en procesos cognitivos de alto nivel —como la navegación mediante SLAM—, este sistema aborda el control a bajo nivel, equiparable a los \textbf{reflejos motores} de un ser vivo. No se trata de "pensar" una trayectoria a largo plazo, sino de reaccionar en milisegundos para corregir desviaciones imperceptibles. Esta capacidad de respuesta inmediata es la que garantiza que el robot sea capaz de interactuar con el mundo físico de forma segura, sirviendo de base fundamental sobre la que se construyen las capas de inteligencia superior. 

\newpage
\section{Punto de partida}
El trabajo se realiza sobre un equipo educativo clásico: el \textbf{Digital Pendulum 33-005} de la marca \textit{Feedback Instruments Ltd}. Este sistema se divide en dos partes: el controlador 33-201 (la caja con la electrónica) y la unidad mecánica 33-200 (el raíl con el carro y la barra).

\begin{figure}[ht]
	\centering
	\includegraphics[width=1.0\textwidth]{Pictures/sistema_original}
	\caption{Digital Pendulum 33-005, descripción esquemática \cite{feedback_getting_started}}
	\label{fig:esquema_sistema}
\end{figure}

En la Figura \ref{fig:esquema_sistema} se detalla la disposición de la unidad mecánica 33-200. El sistema consta de un raíl sobre el que se desplaza horizontalmente un carro metálico. Este movimiento es accionado por un motor de corriente continua situado en un extremo, que transmite la fuerza mediante una correa dentada. En los extremos del raíl se sitúan los finales de carrera, que actúan como interruptores de seguridad para detener el motor antes de que el carro colisione con la estructura. Sobre el carro se monta el eje del péndulo, cuya rotación libre de 360 grados es captada por un sensor óptico (encoder) situado en su base.

Este equipo lleva en los laboratorios de la universidad más de 25 años. Debido a este largo periodo de tiempo, el punto de partida del proyecto se caracteriza por una obsolescencia tecnológica total:
\begin{itemize}
	\item \textbf{Electrónica antigua:} Según la documentación del fabricante, este sistema fue diseñado para operar con \textbf{MATLAB 5 y Windows 95}, utilizando protocolos de comunicación y tarjetas de expansión (bus ISA/PCI) que ya no existen en los ordenadores actuales \cite{feedback_external}. Esto lo convertía en una "caja negra" cerrada e incompatible con las herramientas de programación modernas, lo que justificaba su sustitución completa por un sistema de control digital actual.
\end{itemize}

\begin{figure}[ht]
	\centering
	\includegraphics[width=0.5\textwidth, angle=90]{Pictures/control_viejo}
	\caption{Controlador digital original}
	\label{fig:estado_inicial_fotos}
\end{figure}
\FloatBarrier

\begin{itemize}
	\item \textbf{Falta de mantenimiento:} La unidad mecánica presentaba suciedad acumulada en los raíles y grasa reseca en los engranajes, lo que impedía que el carro se moviera con suavidad, complicando enormemente la tarea de equilibrio. Además de tener un desgaste notable en la correa dentada (transmisión) y el disco óptico del encoder. 
\end{itemize}

\begin{figure}[ht]
	\centering
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Pictures/unidad_mecanica_sin_reparar} % Foto de la electrónica vieja
		\caption{Unidad mecánica con notable falta de mantenimiento}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Pictures/encoder_roto} % Foto de la suciedad en el raíl
		\caption{Encoder del péndulo doblado y con pérdida de pulsos}
	\end{subfigure}
	\caption{Estado inicial de la unidad mecánica}
	\label{fig:estado_unidad_mec}
\end{figure}

\FloatBarrier

Luego el punto de partida es el \textbf{rescate de la unidad mecánica}, aprovechando aquellos elementos que aún conservaban su utilidad, como el motor. No obstante, ha sido necesario intervenir en otros componentes que, debido a su naturaleza como piezas de desgaste, presentaban falta de mantenimiento. Por otro lado, para solucionar el problema del control digital obsoleto, se decidió \textbf{sustituir la electrónica antigua} por tecnología actual (microcontrolador de la familia \textit{C2000 de Texas Instruments} y drivers modernos). Esta elección no solo permite que la unidad vuelva a ser plenamente funcional, sino que además la convierte en una plataforma de \textbf{código abierto}, facilitando que futuros usuarios puedan entender, modificar y mejorar el sistema sin las restricciones del hardware propietario original.

\section{Motivaciones}
Las razones que impulsan el desarrollo de este proyecto combinan el interés personal por ciertos conocimientos adquiridos en el grado y la oportunidad de brindar un servicio a la universidad:

\begin{itemize}
	\item \textbf{Personal y Vocacional:} Mi interés principal dentro del \textit{Grado en Inteligencia Robótica} reside en el control automático, la programación de sistemas empotrados en tiempo real y la electrónica. Este proyecto me permite profundizar en la asignatura de \textit{Sistemas Automáticos Avanzados}, enfrentándome al reto de diseñar algoritmos de control a bajo nivel con restricciones críticas de tiempo y hardware que imponen los sistemas físicos reales.
	
	\item \textbf{Curiosidad Científica:} Me motiva entender el funcionamiento interno de las máquinas. El control automático me brinda la oportunidad perfecta para aplicar de forma práctica la física y las matemáticas que subyacen a los sistemas robóticos capaces de equilibrarse y moverse con precisión. 
	
	\item \textbf{Profesional:} El uso de la familia de microcontroladores \textit{C2000 de Texas Instruments} constituye una motivación clave. Superar las capas de abstracción de \textit{Arduino IDE} para trabajar directamente sobre el registro y el hardware de los \textit{C2000} me permite entender el control de bajo nivel de forma real. Esta capacidad para gestionar sistemas complejos con estándares industriales constituye una ventaja competitiva.
	
	\item \textbf{Académica:} Durante este último curso, he disfrutado de la \textit{Beca de Colaboración del Ministerio} en el \textit{Departamento de Ingeniería de Sistemas Industriales y Diseño}. Parte de mi labor ha consistido en poner en marcha esta infraestructura, por lo que este TFG es la culminación del trabajo. Dejar una unidad funcional y actualizada que sirva como banco de pruebas para que futuros estudiantes e investigadores puedan seguir evolucionando algoritmos y técnicas de control avanzado en la universidad sería un gran honor.
\end{itemize}

\section{Planificación}

Dentro de este apartado se aborda de forma lógica y ordenada los objetivos generales del proyecto y las tareas asociadas para lograr su consecución. Además se expone el \textit{Diagrma de Gantt} para tener una visión general de la planificación de tiempos por cada tarea. 

\subsection{Objetivos}

\begin{itemize}
	\item \textbf{O1:} Obtención del modelo matemático del sistema de péndulo invertido sobre carro.
	
	\item \textbf{O2:} Simulación en \textit{Matlab/Simulink} para la validación preliminar de los controladores e integración con \textit{ROS}.
	
	\item \textbf{O3:} Diseño y evaluación en simulación diferentes estrategias de control.
	
	\item \textbf{O4:} Caracterización y puesta a punto el hardware de la planta real, incluyendo la etapa de potencia y la lectura de sensores.
	
	\item \textbf{O5:} Desarrollo el firmware de bajo nivel en el microcontrolador, garantizando el control en tiempo real.
	
	\item \textbf{O6:} Implementación, ajuste y validación experimental de los algoritmos de control sobre la unidad mecánica real. 
	
	\item \textbf{O7:} Documentación técnica del trabajo realizado y los resultados obtenidos. 
	 
\end{itemize}

\newpage

\subsection{Tareas}

Cada tarea y subtarea tiene un objetivo asociado para facilitar el desarrollo del proyecto en pequeños retos dentro de un plazo. De este modo la organización es mayor y el enfoque del trabajo es claro.  

\subsubsection{T1. Modelado matemático y físico (O1)}
\begin{itemize}
	\item \textbf{T1.1} Obtención de las ecuaciones de movimiento mediante Euler-Lagrange.
	\item \textbf{T1.2} Identificación de parámetros cinemáticos y dinámicos (masas, longitudes, inercias).
	\item \textbf{T1.3} Caracterización de las no linealidades: fricción estática y dinámica en el raíl y zona muerta del motor.
\end{itemize}

\subsubsection{T2. Entorno de simulación e integración con ROS (O2)}
\begin{itemize}
	\item \textbf{T2.1} Implementación del modelo dinámico en bloques de \textit{Simulink}.
	\item \textbf{T2.2} Configuración del nodo de comunicación entre \textit{Matlab} y el ecosistema \textit{ROS} para el intercambio de datos.
	\item \textbf{T2.3} Desarrollo de un entorno visual para la monitorización de la simulación.
\end{itemize}

\subsubsection{T3. Diseño de estrategias de control en simulación (O3)}
\begin{itemize}
	\item \textbf{T3.1} Diseño del control de posición mediante regulador cuadrático lineal (LQR).
	\item \textbf{T3.2} Desarrollo del algoritmo de \textit{Swing-up} basado en el control de energía.
\end{itemize}

\subsubsection{T4. Preparación del hardware y electrónica (O4)}
\begin{itemize}
	\item \textbf{T4.1} Revisión mecánica y mantenimiento de la unidad \textit{Feedback 33-200} (limpieza, engrasado y ajuste de tensión).
	\item \textbf{T4.2} Integración del driver de potencia y diseño del conexionado con los encoders ópticos.
	\item \textbf{T4.3} Implementación de sistemas de seguridad físicos (finales de carrera) y lógicos.
\end{itemize}

\subsubsection{T5. Desarrollo del firmware de bajo nivel (O5)}
\begin{itemize}
	\item \textbf{T5.1} Configuración de periféricos de captura (eQEP) y generación de señales PWM en el microcontrolador.
	\item \textbf{T5.2} Programación del bucle de control con una rutina de interrupción periódica.
	\item \textbf{T5.3} Implementación del protocolo de comunicación entre el microcontrolador y el ordenador para obtener la telemetría.
\end{itemize}

\subsubsection{T6. Validación experimental en el sistema real (O6)}
\begin{itemize}
	\item \textbf{T6.1} Ejecución y ajuste fino de los controladores (\textit{tuning}) sobre la planta física.
	\item \textbf{T6.2} Comparativa de rendimiento entre los resultados obtenidos en el laboratorio y la simulación previa.
\end{itemize}

\subsubsection{T7. Documentación y cierre (O7)}
\begin{itemize}
	\item \textbf{T7.1} Elaboración de la memoria técnica del proyecto.
	\item \textbf{T7.2} Preparación de la defensa pública y material visual de apoyo.
\end{itemize}

\newpage

\subsection{Estimación de tiempos}
% Aquí irían los objetivos (Estabilización, Swing-up, etc.) 
% Y la tabla de horas que ya definimos (300h totales, 50h para memoria).

\section{Estimación de costes}
% Aquí incluyes el coste del ingeniero (25€/h x 300h), 
% el coste proporcional del equipo Feedback y el software MATLAB.

\chapter{Análisis}

En este capítulo se realiza un estudio detallado de los pilares que sustentan el desarrollo del sistema de control para el péndulo invertido. El análisis se articula en tres ejes fundamentales:

\begin{itemize}
	\item \textbf{Análisis de requisitos:} Donde se formalizan las capacidades operativas (funcionales) y las restricciones técnicas, de seguridad y rendimiento (no funcionales) que debe satisfacer el sistema.
	
	\item \textbf{Diseño del sistema:} Se describe la estrategia lógica y matemática adoptada, detallando el modelo dinámico, las leyes de control híbridas y la lógica de conmutación necesaria para el \textit{swing-up} y la estabilización.
	
	\item \textbf{Arquitectura del sistema:} Se define la infraestructura tanto de simulación como de implementación real, especificando el flujo de datos, la integración con ROS y la selección de los componentes de hardware que garantizan la viabilidad del proyecto.
	 
\end{itemize}
\newpage
\section{Requisitos funcionales}
Los requisitos funcionales describen las acciones, cálculos y manipulaciones de datos específicos que el sistema debe realizar para cumplir su propósito. Para el control del péndulo invertido sobre carro se definen mediante sus entradas, comportamiento y salidas:

\begin{itemize}
	\item \textbf{RF1. Modelado y simulación de la dinámica:} 
	\begin{itemize}
		\item \textit{Entradas:} Parámetros físicos del sistema (masas, inercias, coeficientes de fricción) y señal de control simulada.
		\item \textit{Comportamiento:} Resolución matemática de las Ecuaciones Diferenciales Ordinarias (EDO) que rigen la dinámica no lineal del mecanismo en el entorno MATLAB/Simulink.
		\item \textit{Salidas:} Estado cinemático simulado del sistema (posiciones y velocidades articulares).
	\end{itemize}
	
	\item \textbf{RF2. Visualización y telemetría en ROS:}
	\begin{itemize}
		\item \textit{Entradas:} Estado cinemático del sistema (proveniente de la simulación o del hardware real) y archivo de descripción geométrica URDF.
		\item \textit{Comportamiento:} Traducción y publicación de los datos mediante el puente de comunicación de ROS Toolbox para actualizar el árbol de transformadas (TF) del robot.
		\item \textit{Salidas:} Visualización 3D actualizada en tiempo real mediante la interfaz RViz (tópico \textit{/joint\_states}).
	\end{itemize}
	
	\item \textbf{RF3. Control de inyección de energía (Swing-up):} 
	\begin{itemize}
		\item \textit{Entradas:} Posición y velocidad angular actual del péndulo ($q, \dot{q}$).
		\item \textit{Comportamiento:} Cálculo y ejecución de una ley de control basada en energía para balancear el péndulo desde su posición estable ($q = 0 \text{ rad}$) hasta el punto de linealización del sistema ($q = \pi \text{ rad}$).
		\item \textit{Salidas:} Acción de control ($u$).
	\end{itemize}
	
	\item \textbf{RF4. Control de estabilización (Realimentación del estado):} 
	\begin{itemize}
		\item \textit{Entradas:} Vector de estado del sistema ($x = [x, \dot{x}, \theta, \dot{\theta}]^T \equiv [q, \dot{q}]^T$). 
		\item \textit{Comportamiento:} Estabilización del sistema sobre el punto de linealización utilizando control por realimentación del estado. 
		\item \textit{Salidas:}  Acción de control ($u$).
	\end{itemize}
	
	\item \textbf{RF5. Adquisición y decodificación de señales:} 
	\begin{itemize}
		\item \textit{Entradas:} Señales eléctricas de pulsos en cuadratura (A y B) provenientes de los encoders del motor y del péndulo.
		\item \textit{Comportamiento:} Procesamiento y conteo de los pulsos digitales mediante el hardware específico.
		\item \textit{Salidas:} Valores digitales discretos de posición y velocidad interpretables por la ley de control.
	\end{itemize}
\end{itemize}

\section{Requisitos no funcionales}
Los requisitos no funcionales imponen condiciones y restricciones a la implementación del diseño:

\begin{itemize}
	\item \textbf{RNF1. Restricciones de rendimiento (Determinismo y tiempo real):} El bucle de control debe ejecutarse dentro de una rutina de interrupción periódica en un microcontrolador, garantizando un periodo de muestreo estable y un \textit{jitter} despreciable para la viabilidad del control discreto.
	
	\item \textbf{RNF2. Restricciones de fiabilidad (Robustez):} El sistema debe rechazar perturbaciones externas moderadas y ser tolerante a las discrepancias producidas por dinámicas no modeladas (fricciones no lineales y holguras mecánicas).
	
	\item \textbf{RNF3. Restricciones de seguridad (Gestión de fallos):} 
	\begin{itemize}
		\item \textit{Seguridad mecánica:} El software debe implementar una parada de emergencia si las lecturas de posición indican que el carro excede los límites físicos del riel. En caso de fallo en el software de parada se debe disponer de finales de carrera para cortar la alimentación del actuador. 
		\item \textit{Seguridad eléctrica:} Se debe garantizar el aislamiento galvánico entre la etapa de control (3.3V) y la etapa de potencia (24V).
	\end{itemize}
	
	\item \textbf{RNF4. Condiciones de implementación (Abstracción HAL):} El código desarrollado debe hacer uso de la capa de abstracción de hardware (HAL) ofrecida por el fabricante del microcontrolador, asegurando la portabilidad, estandarización y eficiencia del firmware.
\end{itemize}

\newpage
\section{Diseño del sistema}

\begin{figure}[ht]
	\centering
	\input{Pictures/figuras/arquitectura_control.tex}
	\caption{Arquitectura de control}
	\label{fig:arquitectura_control}
\end{figure}

\FloatBarrier

La arquitectura de control propuesta en la Figura \ref{fig:arquitectura_control} se aplica sobre un sistema subactuado tipo SIMO (Single Input Multiple Output). Para la etapa de realimentación, se implementa un observador del estado por inversión. Mediante este enfoque, los estados del sistema se obtienen exclusivamente a partir de las mediciones de salida, dado que, en el caso concreto de sistemas robóticos, el estado coincide con las salidas cinemáticas ($q, \dot{q}$).

\subsection{Introducción al modelado dinámico}

A diferencia de la cinemática, que estudia el movimiento de un mecanismo sin considerar las fuerzas y pares que lo producen, el modelado dinámico describe explícitamente la relación entre la fuerza aplicada y el movimiento resultante \cite{spong2006robot}. Disponer de las ecuaciones de movimiento exactas es un requisito estricto y fundamental tanto para la simulación del sistema como para el diseño de controladores basados en modelo. 

Para determinar la dinámica del péndulo invertido sobre carro, se recurre a las ecuaciones de Euler-Lagrange. Este enfoque energético se basa en la obtención del Lagrangiano del sistema ($L$), definido como la diferencia entre la energía cinética ($E_c$) y la energía potencial ($E_p$):
\begin{equation}
		L = E_c - E_p
	\label{eq:lagrangiano}
\end{equation}

Las ecuaciones diferenciales no lineales resultantes pueden reescribirse y compactarse de forma elegante en la formulación matricial clásica para sistemas robóticos \cite{murray1994mathematical}:
\begin{equation}
		M(q)\ddot{q} + C(q,\dot{q})\dot{q} + G(q) = B u
	\label{eq:matrices_dinamica}
\end{equation}

Para el sistema en concreto, las dimensiones de cada matriz son:
\begin{itemize}
	\item $M(q) \in \mathbb{R}^{2 \times 2}$ matriz de inercia.
	\item $C(q,\dot{q}) \in \mathbb{R}^{2 \times 2}$ matriz de fuerzas centrípetas y de Coriolis. Incluye también el rozamiento viscoso.
	\item $G(q) \in \mathbb{R}^{2 \times 1}$ vector de fuerzas gravitacionales.
	\item $B \in \mathbb{R}^{2 \times 1}$ vector de entrada, mapea la acción de control $u$.
\end{itemize}

Este enfoque matricial no solo facilita el análisis teórico, sino que permite realizar correcciones en la simulación del sistema muy fácilmente, ya que los parámetros que lo caracterizan (masas, longitudes, inercias, etc.) se encapsulan dentro de las propias matrices.  

\subsection{Modelo dinámico del sistema}
\label{cap:modelado_teoria}
El primer paso para el modelado dinámico consiste en definir un sistema de referencia en el plano bidimensional $(x, y)$. Esto permite localizar la posición del centro de masas (CM) del péndulo en función de las coordenadas generalizadas del sistema: la posición lineal del carro ($x$) y la posición angular del péndulo ($\theta$).

Considerando $l$ como la distancia desde el eje de rotación hasta el centro de masas del péndulo, sus coordenadas son:

\begin{equation}
	\begin{cases}
		x_{\text{cm}} = x + l \sin(\theta) \\
		y_{\text{cm}} = -l \cos(\theta)
	\end{cases}\label{eq:coordenadas_cm}
\end{equation}

Esta elección de coordenadas establece de forma implícita el marco de trabajo del controlador. Se define el péndulo colgando hacia abajo como el origen angular ($\theta = 0$ rad) y de acuerdo con la geometría de la Ecuación \ref{eq:coordenadas_cm}, se asume una convención de signos donde el incremento de $\theta$ es en sentido antihorario.


 \begin{figure}[htpb]
 	\centering
	\input{Pictures/figuras/pendulo_carro_cm.tex}
 	\caption{Esquema cinemático, definición del sistema de coordenadas}
 	\label{fig:esquema_pendulo}
 \end{figure}

\FloatBarrier

Una vez establecida la convención de signos del sistema y tener una visión esquemática ofrecida en la Figura \ref{fig:esquema_pendulo}, es momento de desarrollar la ecuación \ref{eq:lagrangiano} hasta obtener la forma matricial compacta de la Ecuación \ref{eq:matrices_dinamica}. La energía potencial y cinética del sistema en concreto se puede representar como \cite{tedrake2023}:
\begin{equation}
	\begin{cases}
		E_{\text{c}} = \frac{1}{2}(M + m)\dot{x}^2 + m\dot{x}\dot{\theta} l\cos(\theta) + \frac{1}{2}  ml^2\dot{\theta}^2 \\
		E_{\text{p}} = -mgl\cos(\theta)
	\end{cases}\label{eq:energia_sistema}
\end{equation}

A partir de formular la energía cinética y potencial, se desarrolla el Lagrangiano (Anexo \ref{anexo:desarrollo_dinamico}) y se calculan sus derivadas parciales hasta obtener las siguientes ecuaciones de movimiento \cite{tedrake2023}:
\begin{equation}
	\begin{cases}
		(M + m)\ddot{x} + ml\ddot{\theta}\cos(\theta) - ml\dot{\theta}^2\sin(\theta) = u \\
		ml\ddot{x}\cos(\theta) + ml^2\ddot{\theta} + mgl\sin(\theta) = 0
	\end{cases}\label{eq:ec_movimiento}
\end{equation}

Antes de compactar las ecuaciones en la forma matricial de la Ecuación \ref{eq:matrices_dinamica} es importante añadir los términos de rozamiento viscoso para el riel y el péndulo.  
\begin{equation}
	\begin{cases}
		(M + m)\ddot{x} + ml\ddot{\theta}\cos(\theta) + c_x\dot{x} - ml\dot{\theta}^2\sin(\theta) = u \\
		ml\ddot{x}\cos(\theta) + ml^2\ddot{\theta} + c_{\theta}\dot{\theta} + mgl\sin(\theta) = 0
	\end{cases}\label{eq:ec_movimiento_final}
\end{equation}

Finalmente se compacta la expresión en forma matricial como indica la Ecuación \ref{eq:matrices_dinamica}. La caracterización del sistema que posteriormente se implementará en simulación es: 
\begin{equation}
	\begin{bmatrix} 
		M + m & m l \cos\theta \\ 
		m l \cos\theta & I + m l^2 
	\end{bmatrix} 
	\begin{bmatrix} 
		\ddot{x} \\ 
		\ddot{\theta} 
	\end{bmatrix} 
	+ 
	\begin{bmatrix} 
		c_x & -m l \dot{\theta} \sin\theta \\ 
		0 & c_\theta 
	\end{bmatrix} 
	\begin{bmatrix} 
		\dot{x} \\ 
		\dot{\theta} 
	\end{bmatrix} 
	+ 
	\begin{bmatrix} 
		0 \\ 
		m g l \sin\theta 
	\end{bmatrix} 
	= 
	\begin{bmatrix} 
		1 \\ 
		0 
	\end{bmatrix} u
	\label{eq:matrices_dinamica_desarrollada}
\end{equation}

Si se analiza la Ecuación \ref{eq:matrices_dinamica_desarrollada} se deducen tres características clave del sistema: 
\begin{itemize}[]
	\item \textbf{Sistema subactuado:} La acción de control $u$ solo permite empujar el carro de forma horizontal, no hay control directo en el posicionado del péndulo. 
	
	\item \textbf{Acoplamiento de términos:} El movimiento del carro afecta a la posición del péndulo y a su vez el movimiento del péndulo actúa como una perturbación en el posicionado del carro. 
	
	\item \textbf{Sistema inestable:} Si se analiza el sistema en $\theta = \pi\ rad$ presenta polos con parte real positiva debido al término $	m g l \sin\theta $. Por tanto, se requiere aplicar acciones de control en bucle cerrado para poder permanecer en esta posición. 
	
\end{itemize}

 \begin{figure}[htpb]
	\centering
	\input{Pictures/figuras/modelado_dinamica_bloques.tex}
	\caption{Diagrama de bloques del modelado}
	\label{fig:bloques_modelado}
\end{figure}

\FloatBarrier

Finalmente, en la Figura \ref{fig:bloques_modelado} se muestra la representación del modelado dinámico del sistema con diagramas de bloques. Es una forma interesante de representación, ya que la traslación a \textit{Simulink} es directa, sirviendo como preámbulo para la implementación de la simulación en este entorno.


\newpage
\subsection{Estrategia de control Swing-up}
\label{cap:swing_up}
La primera ley de control se centra en el levantamiento del péndulo desde su posición estable ($\theta = 0\ rad$) hasta su posición inestable y punto en el que se conmuta al control de posición ($\theta = \pi\ rad$). 

La idea intuitiva tras las matemáticas es simple, consiste en balancear el péndulo inyectando energía a favor del movimiento. El objetivo es llegar al punto de estabilización con pequeñas acciones de control que producen oscilaciones de amplitud creciente en el sistema. 
\begin{figure}[ht]
	\centering
	\input{Pictures/figuras/swing_up_simple.tex}
	\caption{Esquema del control por balanceo (swing-up) basado en energía.}
	\label{fig:swing_up_simple}
\end{figure}

Para formalizar la ley de control esquematizada en la Figura \ref{fig:swing_up_simple}, se define la energía del péndulo libre que viene dada por la suma de la energía potencial y la cinética \cite{astrom2000swinging}: 
\begin{equation}
		E_m = E_c + E_p = \frac{1}{2}J\dot{\theta}^2 - mgl\cos(\theta) 
	\label{eq:pendulo_simple}
\end{equation}

En el caso de la Ecuación \ref{eq:pendulo_simple} se define la máxima energía potencial cuando $\theta = \pi\ rad$ de forma que el sistema de referencia del control coincide con el del modelado. Una vez se formaliza el modelo energético, se determina la referencia energética que el control por balanceo debe conseguir \cite{astrom2000swinging}:
\begin{equation}
		E_{ref} = mgl
	\label{eq:energia_ref}
\end{equation}

Por otro lado, el error de seguimiento para calcular la acción de control $u$ viene dado por la diferencia entre la energía actual del péndulo ($E_m$, Ecuación \ref{eq:pendulo_simple}) y la energía de referencia ($E_{ref}$, Ecuación \ref{eq:energia_ref}) \cite{astrom2000swinging}:
\begin{equation}
		e = E_{m} - E_{ref}
	\label{eq:error_energia}
\end{equation}

Finalmente se define la ley de control que permite balancear el péndulo simple hasta su posición inestable, donde la acción de control $u$ es el par aplicado en el eje de rotación \cite{astrom2000swinging}: 
\begin{equation}
		u = K\dot{\theta}\cos(\theta)e
	\label{eq:ley_control_swp}
\end{equation}

En función del valor de la ganancia $K$ se conseguirá el objetivo en diferente número de oscilaciones. Valores pequeños de la ganancia requieren de más iteraciones para llegar hasta el punto de estabilización. 

Sin embargo, la ley de control definida en la Ecuación \ref{eq:ley_control_swp} no es directamente transferible al péndulo invertido sobre carro. Los motivos son los siguientes:

\begin{itemize}[topsep=5pt, itemsep=4pt] % Ajuste fino de espacios
	\item \textbf{Acción de control:} En la ley de control propuesta en \ref{eq:ley_control_swp} el actuador se encuentra en el eje del péndulo. El sistema definido en \ref{eq:matrices_dinamica_desarrollada} es subactuado y solo se tiene control sobre la fuerza de empuje del carro. Para aplicar la estrategia de energía, es necesario convertir la acción de control virtual (el par necesario en el eje) a una aceleración real del carro a través del acoplamiento dinámico del sistema.
	
	\item \textbf{Límites físicos del riel:} Se hace indispensable, por tanto, un control multiobjetivo que regule la energía del péndulo garantizando simultáneamente la estabilidad de la posición del carro dentro del espacio de trabajo del riel.
\end{itemize}

Para abordar las limitaciones mencionadas, se recurre a la técnica de \textbf{Linearización por Realimentación Parcial (PFL)}\cite{spong1994}. Permite transformar linealizar la dinámica de un subconjunto del sistema mediante un cambio de variables y una ley de control no lineal, siempre que exista un acoplamiento inercial suficiente entre las coordenadas actuadas y las pasivas \cite{spong1994}.

En este trabajo se aplica la variante conocida como \textbf{linealización colocalizada} (\textit{Collocated Linearization}), la cual consiste en linealizar el grado de libertad activo del sistema, es decir, aquel donde se ubica físicamente la acción de control \cite{spong1994}. El objetivo es diseñar una ley de control $u (N)$ que cancele las no linealidades de la dinámica del carro para que este se comporte, desde el punto de vista del controlador de alto nivel, como un doble integrador respecto a una aceleración deseada $\ddot{x}_{des} (\frac{m}{s^2})$.
\begin{figure}[ht]
	\centering
	% Aquí cambias \textwidth por 0.7\textwidth (o el tamaño que prefieras)
	\resizebox{\textwidth}{!}{%
		\input{Pictures/figuras/funcionamiento_PFL.tex}
	}
	\caption{Concepto de equivalencia de la Linealización Colocalizada.}
	\label{fig:equivalencia_PFL}
\end{figure}

\FloatBarrier
\newpage

Partimos de las ecuaciones de movimiento del sistema que incluyen los términos de fricción del carro ($c_x$) y del péndulo ($c_{\theta}$):
\begin{equation}
	\begin{cases}
		(M + m)\ddot{x} + ml\ddot{\theta}\cos(\theta) + c_x\dot{x} - ml\dot{\theta}^2\sin(\theta) = u \\
		ml\ddot{x}\cos(\theta) + J\ddot{\theta} + c_{\theta}\dot{\theta} + mgl\sin(\theta) = 0
	\end{cases}\label{eq:ec_mov}
\end{equation}

Donde $J = I + ml^2$ es el momento de inercia del péndulo respecto al eje de rotación. Para eliminar $\ddot{\theta}$ de la ecuación de control, se despeja dicha variable de la dinámica del péndulo \cite{tedrake2023}:
\begin{equation}
		\ddot{\theta} = \frac{1}{J} \left( -mgl\sin\theta - ml\cos\theta\ddot{x} - c_{\theta}\dot{\theta} \right)
	\label{eq:despeje_theta}
\end{equation}

Al imponer la condición de linealización $\ddot{x} = \ddot{x}_{des}$ y sustituir la expresión \ref{eq:despeje_theta} en la ecuación del carro, se obtiene la fuerza $u$ necesaria para anular la dinámica no lineal del subsistema actuado \cite{tedrake2023}:
\begin{equation}
		u = (M + m)\ddot{x}_{des} + ml\cos\theta \left[ \frac{1}{J} (-mgl\sin\theta - ml\cos\theta\ddot{x}_{des} - c_{\theta}\dot{\theta}) \right] - ml\dot{\theta}^2\sin\theta + c_x\dot{x}
	\label{eq:ley_PFL}
\end{equation}


Como se observa, la fuerza física aplicada ($u$) compensa dinámicamente la masa total y los efectos inerciales del péndulo. Esta arquitectura permite definir la aceleración deseada $\ddot{x}_{des}$ como el punto de encuentro donde convergen los dos objetivos de control del sistema \cite{tedrake2023}:

\begin{equation}
	\ddot{x}_{des} = \underbrace{K_e \dot{\theta} \cos\theta (E - E_{ref})}_{\text{Control de Energía (Swing-up)}} - \underbrace{(K_p x + K_d \dot{x})}_{\text{Control de Posición (PD)}}
	\label{eq:aceleracion_deseada}
\end{equation}

La ley de control resultante en la ecuación \ref{eq:aceleracion_deseada} simboliza un compromiso entre la inyección de energía necesaria para el balanceo y la regulación de la posición del carro sobre el riel \cite{tedrake2023}. Se debe ajustar el peso de cada ganancia para obtener un equilibrio óptimo entre ambos controles. 

\begin{figure}[ht]
	\centering
	\resizebox{\textwidth}{!}{%
		\input{Pictures/figuras/esquema_PFL.tex}
	}
	\caption{Diagrama de bloques de la estrategia de Swing-up con linealización parcial (PFL) }
	\label{fig:PFL}
\end{figure}

\FloatBarrier

 \newpage
\subsection{Modelado en el espacio de estados}

Una vez que la estrategia de \textit{swing-up} ha llevado al péndulo a su posición vertical inestable ($\theta \approx \pi\ rad$), se requiere mantener el péndulo equilibrado en su punto superior mientras se regula la posición del carro en un punto de referencia (típicamente el centro del riel, $x = 0$). Para lograr el cometido se opta por aplicar un control por realimentación del estado. Un control de este tipo, requiere una representación en el espacio de estados del modelo dinámico linealizado en el punto de equilibrio. Todos los sistemas bajo este marco se definen como \cite{Penarrocha_Tema7}:
\begin{equation}
	\begin{cases}
		\dot{x}(t) = Ax(t) + Bu(t) \\
		y(t) = Cx(t) + Du(t)
	\end{cases}\label{eq:def_espacio_estados}
\end{equation}

Donde el vector de estados $\mathbf{x}(t) = [x, \theta, \dot{x}, \dot{\theta}]^T \equiv [q, \dot{q}]^T$ define completamente la condición del sistema en cada instante. Los componentes de la representación se describen a continuación \cite{Penarrocha_Tema7}:
\begin{itemize}[]
	\item\textbf{ Matriz de estado ($A \in \mathbb{R}^{4 \times 4}$):} Caracteriza la dinámica interna del sistema y cómo evolucionan los estados (posiciones y velocidades) entre sí cuando no hay acción de control. Sus autovalores son los polos del sistema e indican si es estable.
	
	\item\textbf{ Matriz de entrada ($B \in \mathbb{R}^{4 \times 1}$):} Define cómo la fuerza aplicada al carro ($u$) afecta a la aceleración de cada uno de los estados.
	
	\item \textbf{Matriz de salida ($C \in \mathbb{R}^{4 \times 4}$):} Relaciona los estados internos con las variables medidas. Dado que en este proyecto se dispone de sensores para todas las variables, $C$ se simplifica a la matriz identidad $I$.
	
	\item \textbf{Matriz de transmisión directa ($D$):} Representa el acoplamiento directo entre la entrada y la salida. Para este caso $D$ es cero, ya que la acción de control actúa sobre la dinámica del sistema primero. 
\end{itemize}

Para sistematizar la transición entre el modelo físico no lineal y el diseño del controlador de estabilidad, se parte de la ecuación \ref{eq:matrices_dinamica}. Al aplicar una linealización por Jacobiano alrededor del punto de equilibrio inestable $x = [0, \pi, 0, 0]^T$, las matrices de la dinámica se transforman en matrices de coeficientes constantes evaluadas en dicho punto \cite{Penarrocha_Tema7}:

\begin{itemize}
	\item \textbf{Inercia:} $\bar{M} = M(\bar{q})$
	\item \textbf{Amortiguamiento:} $\bar{C} = C(\bar{q}, 0)$
	\item \textbf{Rigidez (Gravedad):} $\bar{G} = \left. \frac{\partial G(q)}{\partial q} \right|_{q=\bar{q}}$
	\item \textbf{Entrada:} $\bar{B} = B(\bar{q})$
\end{itemize}

Para el caso en particular del péndulo invertido sobre carro, el sistema linealizado en dicho punto es el siguiente: 

\begin{equation}
	\underbrace{\begin{bmatrix} 
		M + m & -m l \\ 
		-m l  & I + m l^2 
	\end{bmatrix}}_{\bar{M}} 
	\begin{bmatrix} 
		\ddot{x} \\ 
		\ddot{\theta} 
	\end{bmatrix} 
	+ 
	\underbrace{\begin{bmatrix} 
		c_x & 0 \\ 
		0 & c_\theta 
	\end{bmatrix}}_{\bar{C}} 
	\begin{bmatrix} 
		\dot{x} \\ 
		\dot{\theta} 
	\end{bmatrix} 
	+ 
	\underbrace{\begin{bmatrix} 
		0 \\ 
		-m g l 
	\end{bmatrix}}_{\bar{G}} 
	= 
	\underbrace{\begin{bmatrix} 
		1 \\ 
		0 
	\end{bmatrix}}_{\bar{B}} u
	\label{eq:matrices_dinamica_lineal}
\end{equation}

El sistema descrito en la ecuación \ref{eq:matrices_dinamica_lineal} es bajo el que se diseñará el control de posición. Contra más lejos esté el péndulo de su vertical menos efectivo será el control, ya que las diferencias del sistema real respecto del lineal empiezan a acentuarse, este es uno de los motivos por el que se hace necesaria la conmutación entre control de posición y swing-up. 

Definiendo el vector de estados como $\mathbf{x} = [q, \dot{q}]^T$, el sistema puede reescribirse en la representación compacta de espacio de estados mediante la siguiente expresión \cite{Penarrocha_Tema7}:

\begin{equation}
	\begin{bmatrix} \dot{q} \\ \ddot{q} \end{bmatrix} = 
	\underbrace{\begin{bmatrix} 
			\mathbf{0} & I \\ 
			\color{red}{-\bar{M}^{-1}\bar{G}} & \color{red}{-\bar{M}^{-1}\bar{C}} 
	\end{bmatrix}}_{A}
	\begin{bmatrix} q \\ \dot{q} \end{bmatrix} + 
	\underbrace{\begin{bmatrix} \mathbf{0} \\ \color{blue}{\bar{M}^{-1}\bar{B}} \end{bmatrix}}_{B} u
	\label{eq:espacio_estados_bloques}
\end{equation}

\begin{figure}[ht]
	\centering
	\resizebox{\textwidth}{!}{%
		\input{Pictures/figuras/modelado_SS.tex}
	}
	\caption{Flujo de trabajo para el modelado}
	\label{fig:modelado_SS}
\end{figure}

Las principales ventajas de aplicar esta técnica para la obtención de la representación del sistema en el espacio de estados son:

\begin{itemize}
	\item \textbf{Abstracción y generalización del modelado:} La técnica es escalable a cualquier sistema que cumpla la dinámica definida en la ecuación \ref{eq:matrices_dinamica}. Permite centrarse en el diseño del controlador en lugar de obtener de forma manual las matrices $A$ y $B$ para cada sistema concreto. 
	
	\item \textbf{Eficiencia en la implementación:} La transición a un marco matricial facilita enormemente la implementación en lenguajes de programación de alto nivel, como \textit{MATLAB} o \textit{Python} (mediante la librería \textit{NumPy}). El programador simplemente define las matrices ($\bar{M}, \bar{C}, \bar{G}\ y\ \bar{B}$) luego el propio lenguaje obtiene en la salida la representación en espacio de estados ($A\ y \ B$). 
\end{itemize}

\newpage

\subsection{Control por realimentación del estado}
\label{cap:control_kx}

Tras definir en la anterior sección el \textit{sistema lineal invariante (LTI)} que define la dinámica del sistema en el punto de equilibrio $x = [0, \pi, 0, 0]^T$, se requiere diseñar una ley de control que permita estabilizar el sistema en esta región. 

El \textbf{control por realimentación del estado} para este caso asume que todos los estados del sistema ($\mathbf{x}$) están disponibles para su medición. Bajo esta premisa, la acción de control $u(t)$ se calcula como una combinación lineal de dichos estados multiplicados por un vector de ganancias $K$, sumado opcionalmente a un término de referencia $r(t)$ escalado por una ganancia $K_r$ \cite{Penarrocha_Tema8}:

\begin{equation}
	u(t) = K_r r(t) - K \mathbf{x}(t)
	\label{eq:ley_control_realimentacion}
\end{equation}

Desglosando el vector de ganancias $K = [K_1, K_2, K_3, K_4]$, la fuerza aplicada al carro se define de forma extendida como:

\begin{equation}
	u = K_r r - (K_1 x + K_2 \theta + K_3 \dot{x} + K_4 \dot{\theta})
	\label{eq:ley_control_realimentacion_exten}
\end{equation}


\begin{figure}[ht]
	\centering
	\resizebox{\textwidth}{!}{%
		\input{Pictures/figuras/realimentacion_estado.tex}
	}
	\caption{Diagrama de bloques de la ley de control por realimentación del estado}
	\label{fig:control_SS}
\end{figure}

La figura \ref{fig:control_SS} muestra como el controlador obtiene la acción de control a aplicar en cada instante de tiempo en función de los estados y la referencia. Por otro lado, en comparación con la ecuación \ref{eq:def_espacio_estados}, el sistema en bucle cerrado representado en espacio de estados es \cite{Penarrocha_Tema8}:
\begin{equation}
	\begin{cases}
		\dot{x}(t) = Ax(t) + B(Kr\cdot r(t) - K \cdot x(t)) = (A -BK)x(t) + BKr(t)\\
		y(t) = Cx(t) + Du(t)
	\end{cases}\label{eq:def_espacio_estados_BC}
\end{equation}

Bajo esta configuración, la dinámica del sistema en bucle cerrado queda gobernada por la expresión matricial $(A - BK)$. Por tanto, el diseño del controlador se reduce a determinar los valores del vector de ganancias $K = [k_1, k_2, k_3, k_4]$ que garanticen que todos los autovalores resultantes posean parte real negativa, dotando así al sistema de una respuesta temporal específica. Para abordar este problema, se consideran habitualmente dos metodologías:
\begin{itemize}
	\item \textbf{Asignación de polos:} Consiste en definir arbitrariamente la ubicación de los autovalores de la matriz $(A-BK)$ en el plano complejo. Se calculan las ganancias de $K$ para que el sistema responda con una dinámica concreta (amortiguamiento y tiempo de establecimiento) \cite{Penarrocha_Tema8}.
	\newpage
	\item \textbf{Regulador Cuadrático Lineal (LQR):} Esta metodología aborda el diseño como un problema de optimización. En lugar de elegir la ubicación de los polos, el diseñador define qué es más costoso: que el sistema se aleje del equilibrio o que el actuador trabaje en exceso \cite{Penarrocha_Tema8}. El algoritmo calcula de forma automática la matriz de ganancias $K$ que minimiza el siguiente índice de coste cuadrático:
	\begin{equation}
		J = \int_{0}^{\infty} (\mathbf{x}^T Q \mathbf{x} + u^T R u) \, dt
		\label{eq:indice_coste_lqr}
	\end{equation}
	El comportamiento del controlador se sintoniza a través de dos matrices de ponderación:
	\begin{itemize}
		\item \textbf{Matriz $Q$ ($4 \times 4$):} Penaliza la desviación de los estados. Valores elevados en la diagonal de $Q$ indican que se prioriza la precisión y la rapidez en la estabilización de los estados ($x, \theta, \dot{x}, \dot{\theta}$).
		\item \textbf{Matriz $R$ ($1 \times 1$):} Penaliza el esfuerzo de control $u(t)$. Un valor de $R$ elevado suaviza la respuesta del actuador.
	\end{itemize}
	Es importante notar que la respuesta del sistema no depende de los valores absolutos de las matrices, sino del peso relativo entre ellas ($Q$ frente a $R$). 
\end{itemize}
 
\subsection{Lógica de conmutación}
\label{cap:log_conmu}
Finalmente, la lógica de conmutación atiende a un umbral angular. Este bloque de decisión monitoriza de forma continua la posición angular estimada del péndulo ($\hat{\theta}$). Si el péndulo ingresa dentro de una vecindad acotada en torno a la vertical absoluta (\textit{el punto de linealización del sistema}), la lógica conmuta al control por realimentación del estado En caso contrario, el sistema mantiene activo el controlador de swing-up para elevar el mecanismo. Este comportamiento híbrido se formaliza en el siguiente pseudocódigo:

\begin{algorithm}[H]
	\caption{Lógica de Conmutación}
	\label{alg:conmutacion}
	\begin{algorithmic}[1]
		\Require Vector de estados estimado $\hat{x} = [\hat{x}_c, \dot{\hat{x}}_c, \hat{\theta}, \dot{\hat{\theta}}]^T$, umbral angular $\theta_{\text{th}}$
		\Ensure Señal de control $u$ a aplicar a la planta
		\Statex
		\Loop
		\State $\hat{\theta} \gets \hat{x}(3)$ \Comment{Obtener posición angular actual}
		\If{$|\hat{\theta}| \leq \theta_{\text{th}}$} 
		\State $u \gets -K \hat{x}$ \Comment{Control de posición}
		\Else 
		\State $u \gets f_{\text{swing}}(\hat{x})$ \Comment{Control de energía}
		\EndIf
		\State 
		\EndLoop
	\end{algorithmic}
\end{algorithm}
\newpage
\section{Arquitectura del sistema}

Para definir la arquitectura del sistema se ha hecho una división entre la parte de simulación y la implementación en un sistema real. 

La arquitectura del sistema de simulación permite obtener una vista general del flujo de trabajo para el modelado y la validación antes de la implementación real. Por otro lado, la arquitectura del sistema real se centra en los periféricos involucrados en el control así como el proceso de discretización que se debe aplicar para controlar un sistema real utilizando un microcontrolador. 

\subsection{Arquitectura de la simulación}

\begin{figure}[ht]
	\centering
	\resizebox{\textwidth}{!}{%
		\input{Pictures/figuras/arquitectura_simulacion.tex}%
	}
	\caption{Arquitectura de la simulación}
	\label{fig:arquitectura_sim}
\end{figure}

\FloatBarrier

La Figura \ref{fig:arquitectura_sim} detalla la arquitectura de software diseñada para la simulación del sistema. A continuación se desglosa cada elemento del esquema para conocer su función y como se conecta con el resto de la arquitectura: 

\begin{itemize}[]
	\item \textbf{Cálculo de EDOs (Motor de Física):} Implementado en el entorno MATLAB/Simulink, este bloque se encarga de resolver las Ecuaciones Diferenciales Ordinarias (EDO) que describen la dinámica del sistema. Simulink actúa como el motor de física, calculando en cada paso de tiempo los estados del robot (posiciones en radianes y metros). Estos datos se transmiten al ecosistema ROS mediante ROS Toolbox, publicando la información en el tópico $/joint\_states$.
	
	\item \textbf{Visualización, gemelo digital:} Este bloque recibe la información cinemática para representar el comportamiento del sistema en tiempo real. Está totalmente integrado en el entorno de ROS y se compone de varias piezas: 
	\begin{itemize}[]
		\item \textbf{Archivo URDF (Unified Robot Description Format):} Es un archivo XML que describe como es el robot o mecanismo que se quiere simular. Se especifican las visuales, las restricciones cinemáticas y los marcos de referencia de las articulaciones.  
		\item \textbf{Robot State Publisher:} Es el nodo de ROS que permite representar el movimiento del robot. Se suscribe al tópico $/joint\_states$ y actualiza el árbol de transformadas de nuestro archivo URDF. 
		\item \textbf{RViz:} La última pieza de la arquitectura software, es la interfaz que permite visualizar de forma gráfica e intuitiva todo el flujo de datos como un modelo 3D en movimiento. 
	\end{itemize}
\end{itemize}

\newpage
\subsection{Arquitectura del sistema real}

\begin{figure}[ht]
	\centering
	\resizebox{\textwidth}{!}{%
		\input{Pictures/figuras/arquitectura_control_sistema_real.tex}%
	}
	\caption{Arquitectura del sistema real}
	\label{fig:arquitectura_sim_fis}
\end{figure}

\FloatBarrier

La Figura \ref{fig:arquitectura_sim_fis} presenta de forma esquemática la implementación del bucle de control en el microcontrolador. 

Esta estructura permite observar la interacción entre el entorno digital (discreto) y el proceso físico (continuo). El núcleo del sistema es el \textbf{Controlador Digital}. Este procesador se encarga de ejecutar el algoritmo de control que procesa la señal de error entre la referencia ($r_k$) y la realimentación ($y_k$). 

El concepto fundamental es el \textbf{periodo de muestreo} ($T$). Este parámetro determina la periodicidad de la rutina de control, permitiendo sincronizar las etapas de medida, cálculo y actuación sobre la planta de manera precisa. Durante el desarrollo del proyecto se determinará el periodo de muestreo atendiendo a factores como la dinámica del sistema en bucle cerrado o la frecuencia de la señal PWM. 

Finalmente la interacción entre el mundo digital y el mundo físico es posible mediante el uso de \textbf{periféricos} que permiten medir el estado del sistema o modificarlo. Diferenciamos en dos clases de periféricos: 
\begin{itemize}[]
	\item \textbf{Actuadores:} La señal de control digital calculada ($u_k$) se traduce en señales lógicas de dirección y una señal modulada por ancho de pulso (\textit{PWM}) generada por el periférico \textit{ePWM}. Tanto el estado de las señales digitales como el ciclo de trabajo (\textit{duty cycle}) del PWM se mantienen constantes durante el intervalo de muestreo ($T_s$). Estas señales excitan un puente en H (driver), el cual conmuta el voltaje de alimentación para inyectar una corriente en el motor. De este modo, se transforma la decisión del algoritmo en el par mecánico necesario para accionar el proceso continuo.
	
	\item \textbf{Sensores:} La respuesta física del sistema ($y_p(t)$) es captada por encoders ópticos incrementales. A diferencia de un sensor analógico convencional, estos dispositivos generan una secuencia de pulsos basada en eventos de movimiento (detección de flancos). El periférico \textit{eQEP} del microcontrolador actúa como una unidad de conteo especializada que registra estos eventos de forma asíncrona. No obstante, para el bucle de control, la señal se digitaliza de forma efectiva ($y_k$) al leer el estado de dichos contadores en intervalos de tiempo estrictamente periódicos ($T_s$). Este muestreo periódico es fundamental para el cálculo de variables derivadas, como la velocidad, mediante la diferencia de posición en cada intervalo.
\end{itemize}
\newpage

\subsection{Especificación de componentes de hardware}

Para la implementación física del sistema de control diseñado, se han seleccionado componentes que garantizan un compromiso óptimo entre precisión de lectura, capacidad de procesamiento y tiempo de respuesta. En la Tabla \ref{tab:componentes_hardware} se resumen los elementos principales del prototipo.

\begin{table}[ht]
	\centering
	\caption{Resumen de componentes del sistema de hardware.}
	\begin{tabular}{lll}
		\hline
		\textbf{Componente} & \textbf{Modelo} & \textbf{Función Principal} \\ \hline
		Microcontrolador & TMS320F280049C & Procesamiento de señales y ejecución del control\\
		Driver de motor & AQMH2407ND & Interfaz de potencia (Puente H con aislamiento) \\
		Encoder Motor & HEDS-5140-I (2048 PPR) & Medición de posición lineal ($x$) \\
		Encoder Péndulo & HEDS-5140-A06 (2000 PPR) & Medición de ángulo vertical ($\theta$) \\
		Actuador & Crouzet 82 760 0 (24V) & Generación de fuerza sobre el carro ($u$) \\ \hline
	\end{tabular}
	\label{tab:componentes_hardware}
\end{table}

\subsubsection{Unidad de Procesamiento (Microcontrolador)}
Para la implementación del algoritmo de control se ha seleccionado el \textbf{TMS320F280049C}, un microcontrolador de tiempo real de la familia \textbf{C2000} de \textbf{Texas Instruments}. Este dispositivo está específicamente diseñado para aplicaciones de control, destacando su núcleo \textit{C28x} a \textbf{100 MHz} que integra una unidad de punto flotante (\textbf{FPU}) y una unidad de aceleración trigonométrica (\textbf{TMU}) \cite{ti_f280049c_ds}. Ambos aceleradores son fundamentales para procesar la aritmética del controlador y las funciones trigonométricas del modelo.

\begin{figure}[ht]
	\centering
	\includegraphics[width=0.7\textwidth]{Pictures/F280049C_launchpad}
	\caption{TMS320F280049C en formato \textit{Launchpad}}
	\label{fig:diagrama_bloques_dsp}
\end{figure}

Asimismo, el chip dispone de periféricos de control avanzado que descargan de tareas críticas a la CPU, los más destacados en el proyecto son:
\begin{itemize}
	\item \textbf{eQEP (Enhanced Quadrature Encoder Pulse):} Dos módulos dedicados para la decodificación de señales en cuadratura de los encoders, permitiendo una lectura de posición y ángulo sin consumo de ciclos de reloj \cite{ti_f280049c_ds}.
	\item \textbf{ePWM (Enhanced Pulse Width Modulator):} El periférico permite una resolución de \textbf{16 bits} en la generación del ciclo de trabajo (\textit{duty cycle}) y hasta \textbf{32 bits} si se utiliza \textit{High Resolution PWM}, garantizando un control fino sobre el motor DC \cite{ti_f280049c_ds}.
\end{itemize}

El desarrollo software se apoya en el ecosistema oficial de Texas Instruments. Como entorno de desarrollo integrado (IDE) se emplea \textbf{Code Composer Studio (CCS)} \cite{ti_ccs}, permite una depuración avanzada y optimización específica para la arquitectura C28x. Además, se utiliza la SDK \textbf{C2000Ware}, que proporciona una capa de abstracción de hardware (HAL) facilitando la programación y la interpretabilidad del código \cite{ti_c2000ware}.

\subsubsection{Etapa de Potencia y Actuación}
La acción de control $u(t)$ se traduce en una tensión aplicada al motor mediante el driver de motores de corriente continua \textbf{AQMH2407ND}. Implementa un \textbf{Puente H} de doble canal basado en tecnología \textbf{MOSFET} \cite{handson_aqmh2407nd}, lo que le otorga una eficiencia energética superior y una disipación térmica significativamente menor en comparación con drivers basados en transistores \textbf{BJT}, como el \textbf{L298N} \cite{st_l298_ds}.

El dispositivo es capaz de suministrar una corriente continua de hasta \textbf{7 A} por canal (con picos de hasta 50 A) \cite{handson_aqmh2407nd}. El control se gestiona mediante señales PWM provenientes del microcontrolador, permitiendo la regulación de la tensión media aplicada al motor.

Un aspecto fundamental en el diseño de este driver es la integración de \textbf{optoacopladores EL357N-G} \cite{everlight_el357n_ds}, los cuales proporcionan un \textbf{aislamiento galvánico} entre la etapa de control y la de potencia. Esta característica es esencial en la implementación de sistemas de control, ya que ofrece:
\begin{itemize}
	\item \textbf{Mitigación de ruido:} Evita que el ruido electromagnético inducido por la conmutación de las bobinas del motor afecte a las señales de medición.
	\item \textbf{Protección del hardware:} Asegura la integridad del microcontrolador frente a posibles picos de tensión o corrientes de retorno (\textit{back-EMF}) provenientes de la etapa de potencia.
\end{itemize}

\begin{figure}[ht]
	\centering
	\includegraphics[width=0.6\textwidth]{Pictures/pte_h}
	\caption{Driver AQMH2407ND}
	\label{fig:pte_h}
\end{figure} 

\newpage

\subsubsection{Sensores (Encoders)}

Para el caso concreto de péndulo invertido sobre carro, se requieren dos encoders (uno por grado de libertad), estos sensores son vitales para obtener el estado del sistema en cada instante de tiempo y calcular una acción de control acorde. 

Concretamente, se ha utilizado un sistema de detección óptica que se compone de un módulo detector de la serie \textbf{HEDS-9140} y un disco óptico de la serie \textbf{HEDS-5140}. A continuación se descibe la ubicación y resolución de los dos encoders del sistema:

\begin{itemize}
	\item \textbf{Posición del carro ($x$):} Se utiliza un encoder acoplado al eje del motor de continua que proporciona una resolución de \textbf{2048 pulsos por revolución (PPR)} tras el procesado en cuadratura. 
	
	\item \textbf{Ángulo del péndulo ($\theta$):} Acoplado en el eje del pénudlo, ofrece 500 ciclos por revolución (CPR), traduciendose en una resolución de \textbf{2000 PPR} en cuadratura.
\end{itemize}

\begin{figure}[ht]
	\centering
	\begin{subfigure}[b]{0.45\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Pictures/nomenclatura_encoders_disco}
		\caption{Especificaciones módulo de detección y disco óptico según modelo específico}
		\label{fig:nomenclatura_disco}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.45\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Pictures/heds_9140_dibujo}
		\caption{Descripción del módulo detector y mapeo de señales}
		\label{fig:heds_dibujo}
	\end{subfigure}
	\caption{Detalles técnicos de los componentes de detección óptica de la serie HEDS \cite{broadcom_heds9140_ds}}
\end{figure}

La Figura \ref{fig:nomenclatura_disco} permite identificar con precisión la configuración de cada sensor atendiendo a su resolución y dimensiones mecánicas:
\begin{itemize}
	\item \textbf{Encoder del motor:} Para alcanzar los 2048 PPR en cuadratura, se utiliza un disco óptico de 512 CPR. Siguiendo la codificación de la figura, este corresponde al modelo \textbf{HEDS-5140-I}.
	
	\item \textbf{Encoder del péndulo:} Este eje utiliza el modelo \textbf{HEDS-5140-A06}. El código \textbf{A} identifica la resolución de 500 CPR (2000 PPR en cuadratura), mientras que el sufijo \textbf{06} indica un diámetro interno del disco, garantizando un ajuste mecánico con el eje sin holguras.
\end{itemize}

Por último, el módulo detector \textbf{HEDS-9140} (Figura \ref{fig:heds_dibujo}) es el encargado de transformar el paso de las ranuras del disco en señales TTL. La disposición de sus fotodetectores internos permite que el periférico \textbf{eQEP} del microcontrolador interprete el desfase entre los canales A y B para determinar la dirección del movimiento.

\newpage

\subsubsection{Actuador}
El movimiento del carro se realiza mediante un motor de corriente continua de imanes permanentes del fabricante \textbf{Crouzet}, modelo \textbf{82 760 0}. 

\begin{table}[ht]
	\centering
	\caption{Parámetros técnicos del motor Crouzet 82 760 0 (24V) \cite{crouzet_82760_ds}}
	\label{tab:datos_motor}
	\begin{tabular}{lcc}
		\hline
		\textbf{Parámetro} & \textbf{Valor} & \textbf{Unidad} \\ \hline
		Velocidad nominal & 2300 & rpm \\
		Par nominal & 76 & mN$\cdot$m \\
		Potencia útil nominal & 18 & W \\
		Corriente nominal & 1,3 & A \\
		Resistencia ($R$) & 5,5 & $\Omega$ \\
		Inductancia ($L$) & 7,2 & mH \\
		Constante de par ($K_t$) & 0,068 & N$\cdot$m/A \\
		Inercia ($J_m$) & 270 & g$\cdot$cm$^2$ \\ 
		Eficiencia ($\eta$) & 57 & \% \\ \hline
	\end{tabular}
\end{table}

Los datos del fabricante expuestos en la tabla \ref{tab:datos_motor} son vitales para caracterizar actuador y estimar la relación entre el voltaje aplicado en sus bornes y la fuerza horizontal que empuja el carro. 

\begin{figure}[ht]
	\centering
	% Primera imagen (el plano)
	\begin{subfigure}{\textwidth}
		\centering
		\includegraphics[width=0.6\textwidth]{Pictures/motor_plano}
		\caption{Dimensiones del actuador \cite{crouzet_82760_ds}}
		\label{fig:plano_motor}
	\end{subfigure}
	
	\vspace{0.5cm} % Espacio controlado entre las dos imágenes
	
	% Segunda imagen (la real)
	\begin{subfigure}{\textwidth}
		\centering
		\includegraphics[width=0.5\textwidth]{Pictures/actuador_real}
		\caption{Motor instalado en la unidad mecánica del laboratorio}
		\label{fig:motor_real}
	\end{subfigure}
	
	\caption{Detalles del actuador Crouzet 82 760 0: diseño técnico y montaje real.}
	\label{fig:grupo_motor}
\end{figure}

El sistema utiliza una transmisión por correa dentada (Figura \ref{fig:motor_real}) para convertir el movimiento rotativo del motor en el desplazamiento lineal del carro, minimizando las holguras mecánicas.

\chapter{Desarrollo}

Explicación de las actividades realizadas durante el desarrollo del proyecto. Se puede exponer en orden cronológico o por grupos de tareas. Hay que indicar los hitos alcanzados.

En esta sección solamente hay que aportar la información técnica estrictamente necesaria para comprender el trabajo realizado. Esta información debe, además, estar presentada de forma que sea inteligible por cualquiera no familiarizado con los aspectos técnicos del trabajo realizado. La información técnica puede ubicarse en los apéndices para ser consultada si se desea.

\newpage
\section{Trabajo de simulación}

\subsection{Modelado del sistema en Simulink}
\label{cap:modelado_sim}
El punto de partida consistió en trasladar el modelo representado en la Ecuación \ref{eq:matrices_dinamica_desarrollada} hacia un entorno de simulación numérica en \textit{MATLAB/Simulink}. Para una primera aproximación, se emplearon los parámetros de masas, longitudes, inercias y rozamientos proporcionados en la documentación original del fabricante, los cuales se detallan en la Tabla \ref{tab:parametros_fabricante}. No obstante, debido a que la unidad mecánica cuenta con más de 25 años de antigüedad, posteriormente se llevó a cabo la identificación experimental de sus parámetros reales.

\begin{table}[ht]
	\centering
	\caption{Parámetros proporcionados por el fabricante \cite{feedback_pendulum_33005_manual}}
	\label{tab:parametros_fabricante}
	\begin{tabular}{l c c l}
		\toprule
		\textbf{Parámetro} & \textbf{Símbolo} & \textbf{Valor} & \textbf{Unidades} \\
		\midrule
		Masa del carro              & $M$         & 1.12   & $\mathrm{kg}$ \\
		Masa del péndulo            & $m$         & 0.12   & $\mathrm{kg}$ \\
		Longitud al centro de masas & $l$         & 0.305  & $\mathrm{m}$ \\
		Inercia del péndulo         & $J_p$       & 0.0136 & $\mathrm{kg \cdot m^2}$ \\
		Fricción del carro           & $c_x$       & 0.05   & $\mathrm{N \cdot s / m}$ \\
		Fricción del péndulo        & $c_\theta$  & 0.0005 & $\mathrm{N \cdot m \cdot s / rad}$ \\
		\bottomrule
	\end{tabular}
\end{table}

Este enfoque metodológico responde firmemente al paradigma del \textit{Diseño Basado en Modelos} (MBD). Disponer de un modelo matemático bien calibrado para asegurar una transición eficiente entre la teoría y la práctica. Al emular con precisión el comportamiento de la planta real, el entorno virtual permite verificar de manera exhaustiva la estabilidad y robustez de los algoritmos diseñados antes de su implementación en el hardware, mitigando por completo el riesgo de daños mecánicos o eléctricos por pérdidas de control. 

\begin{figure}[ht]
	\centering
	\includegraphics[width=1.0\textwidth]{Pictures/respuesta_libre_sim}
	\caption{Diagrama de bloques del modelo dinámico implementado en \textit{Simulink}}
	\label{fig:sim_respuesta_libre}
\end{figure} 

La Figura~\ref{fig:sim_respuesta_libre} detalla la arquitectura implementada en \textit{Simulink}, la cual constituye la traslación directa de las ecuaciones dinámicas del sistema al entorno computacional, en el Anexo \ref{ap:codigo_dinamica} se detalla el contenido de cada bloque. Esta estructura materializa de forma práctica el álgebra de bloques conceptual introducida previamente en la Figura~\ref{fig:bloques_modelado}, sirviendo como la plataforma sobre la cual se ejecutan los algoritmos de control por realimentación del estado y por energía.

\subsection{Algoritmo de control en Simulink}

\subsubsection{Control de posición}
Una vez modelada la dinámica del sistema de péndulo invertido sobre carro e implementada en \textit{Simulink}, el siguiente paso fue implementar un control de posición por realimentación del estado para mantener el sistema estable con el péndulo invertido ($\theta = \pi \ \mathrm{rad}$). 
\begin{figure}[ht]
	\centering
	\includegraphics[width=0.9\textwidth]{Pictures/control_posicion}
	\caption{Diagrama de bloques con control de posición por realimentación del estado}
	\label{fig:sim_control_lqr}
\end{figure}

Como se ilustra en la Figura~\ref{fig:sim_control_lqr}, la arquitectura implementada establece una dinámica en bucle cerrado donde el bloque del controlador procesa de forma continua los vectores de estado medidos ($q$ y $\dot{q}$). A partir de esta información, el algoritmo computa la acción de control óptima, denotada como la fuerza de empuje $u$. El fundamento matemático detrás de esta técnica fue abordado en el desarrollo teórico de la Sección~\ref{cap:control_kx}.

La ley de control se reduce conceptualmente a una combinación lineal de los estados multiplicados por un vector de ganancias $K$, expresado formalmente como $u = K(\mathrm{ref} - x)$. 

Consecuentemente, como se aprecia en el detalle del diagrama de bloques, esta ley de control se materializa en \textit{Simulink} mediante un único bloque de ganancia matricial (denotado en la simulación como \texttt{K*u}).

Para concluir, la obtención numérica de los vectores de ganancia $K$ dentro del entorno de \textit{MATLAB}, se optó por las dos metodologías clásicas de control propuestas en la Sección \ref{cap:control_kx}:
\begin{itemize}[]
	\item \textbf{Regulador Lineal Cuadrático (LQR):} se definieron las matrices de ponderación $Q$ y $R$ para sintonizar el comando \texttt{lqr()} \cite{matlab_lqr}.
	
	\item \textbf{Asignación de polos:} Se fija la dinámica en bucle cerrado del sistema con el comando \texttt{place()} \cite{matlab_place}.
\end{itemize}

\begin{matlabbox}{Comando lqr() y comando place() para la obtención de K}
	%% OBTENCION  DE K = [K1, K2, K3 K4]
	% 1. Regulador Lineal Cuadratico (LQR)
	Q = diag([1, 1, 1, 1]); 
	R = 0.1;                
	[K_lqr, S, E] = lqr(A, B, Q, R);
	
	% 2. Asignacion de Polos (Pole Placement)
	polos = [-2.5, -3, -3.5, -4]; 
	K_plc = place(A, B, polos);
\end{matlabbox}



\subsubsection{Levantamiento de péndulo}
El siguiente hito en el trabajo, fue conseguir levantar el péndulo con la técnica de \textit{Swing-Up} abordada de forma teórica en la Sección \ref{cap:swing_up}. Para conseguirlo se utilizó la misma metodología que para el control de posición del péndulo, se tiene un conjunto de bloques que representan la dinámica del sistema y un bloque para el controlador. 

\begin{figure}[ht]
	\centering
	\includegraphics[width=1.0\textwidth]{Pictures/control_energia}
	\caption{Diagrama de bloques con control de energía para el levantamiento del péndulo}
	\label{fig:control_energia}
\end{figure}
\FloatBarrier

Atendiendo a la Figura \ref{fig:control_energia} el bloque de simulación tiene dos subconjuntos claramente diferenciados:
\begin{itemize}[]
	\item \textbf{Control de energía (Zona Izquierda):} Se utiliza una \textit{MATLAB Function} para integrar de forma compacta la técnica de Swing-Up con PFL, tiene como entradas los estados del sistema ($q, \dot{q}$) y la energía de referencia ($E_{ref}$), como salida proporciona una acción de control ($u$) en forma de fuerza de empuje horizontal. Para conocer la implementación del controlador en más detalle consulte el Anexo \ref{ap:control_energia}.
	
	\item \textbf{Modelo Dinámico (Zona Derecha):} Representa la planta del sistema con los parámetros que la caracterizan. Los detalles de la implementación y el bloque de código de cada función se abordan en la Sección \ref{cap:modelado_sim} y el Anexo \ref{ap:codigo_dinamica}.
\end{itemize}
\newpage
\subsubsection{Conmutación entre controladores}
Una vez implementados ambos controladores por separado, para finalizar con la implementación en \textit{Simulink} se aborda la lógica de conmutación explicada en la Sección \ref{cap:log_conmu}. 
\begin{figure}[ht]
	\centering
	\includegraphics[width=0.9\textwidth]{Pictures/conmutacion_controles}
	\caption{Diagrama de bloques conmutación entre controles}
	\label{fig:sim_control_conmutado}
\end{figure}
\FloatBarrier

La arquitectura implementada en \textit{Simulink} (Figura~\ref{fig:sim_control_conmutado}) representa el hito central del desarrollo de la simulación. Su diseño permite emular el comportamiento físico del péndulo e integrar de forma segura las estrategias de control antes de su transferencia al hardware real. Para facilitar su comprensión, el diagrama se divide en tres bloques funcionales:

\begin{itemize}
	\item \textbf{Modelo Dinámico de la Planta (Zona Derecha):} Tal y como se comentó durante el capítulo representa el comportamiento mecánico del sistema.
	
	\item \textbf{Algoritmos de Control (Zona Izquierda):} Resuelven el desafío de control mediante dos estrategias diferenciadas según el estado del péndulo:
	\begin{itemize}
		\item \textbf{Control de Energía (Swing-up):} Actúa cuando el péndulo está hacia abajo, balanceándolo de un lado a otro para inyectarle la energía necesaria para elevarlo.
		\item \textbf{Control de Posición:} Actúa mediante realimentación de estados para mantener el péndulo en equilibrio estricto una vez ha alcanzado la posición vertical inestable.
	\end{itemize}
	
	\item \textbf{Lógica de Conmutación (Zona Central):} Es el componente que resuelve la transición automática entre ambos controladores. El bloque \texttt{q\_norm} normaliza el ángulo del péndulo entre $[-\pi, \pi]$; si este se encuentra lejos de la vertical, el interruptor (\texttt{Switch}) selecciona el control de energía. En el instante en que el péndulo entra en la zona de equilibrio (supera cierto umbral angular), el dispositivo conmuta automáticamente al control de posición.
\end{itemize}

Tras la integración de la lógica de conmutación de ambos controles, la parte de cálculo numérico para las \textit{EDOs} queda resuelta, se pueden ajustar controladores y observar el comportamiento del sistema únicamente con esta implementación. No obstante, atendiendo a la Figura \ref{fig:arquitectura_sim} sobre la \textit{Arquitectura de la Simulación} falta implementar la conexión con el entorno de \textit{ROS} para la visualización del gemelo digital en \textit{RViz}. 
\newpage

\subsection{Comunicación con ROS y Visualización del Gemelo Digital}

Una vez resuelto el cálculo dinámico y el control en Simulink, el siguiente hito fundamental consistió en establecer la infraestructura de comunicación con el ecosistema de ROS. Esta integración permite materializar el concepto de gemelo digital, facilitando la visualización tridimensional del sistema en tiempo real.

Para articular esta conexión se empleó la herramienta \textit{ROS Toolbox}, estructurando el flujo de información bajo el paradigma de publicador/suscriptor:
\begin{itemize}
	\item \textbf{Lógica de Publicación (Nodo Publicador):} Como se ilustra en la Figura~\ref{fig:nodo_publicador}, el nodo desarrollado en Simulink toma de forma continua el vector con las posiciones $x$ y $\theta$ (\texttt{[q]}) y el tiempo de simulación para estructurar el envío de datos mediante un bloque de conversión (\texttt{MATLAB to ROS}). Esta información se encapsula en la estructura de un mensaje dedicado a articulaciones (\texttt{sensor\_msgs/JointState}) y el bloque \texttt{Publish} la difunde activamente en la red bajo el tópico \texttt{/joint\_states}.
\end{itemize}
\begin{figure}[ht]
	\centering
	\includegraphics[width=0.8\textwidth]{Pictures/nodo_publicador}
	\caption{Nodo publicador en Simulink}
	\label{fig:nodo_publicador}
\end{figure}
\FloatBarrier

\begin{itemize}
	\item \textbf{Modelado y Visualización en RViz:} Se diseñó un archivo \textit{URDF} que define la geometría y las articulaciones mecánicas del carro y el péndulo. La herramienta de visualización \textit{RViz} se suscribe al tópico generado por Simulink, traduciendo instantáneamente cada mensaje numérico recibido en movimiento visual sobre el modelo 3D.
\end{itemize}
\begin{figure}[ht]
	\centering
	\includegraphics[width=0.7\textwidth]{Pictures/modelo_3D}
	\caption{Visualización del modelo 3D en RViz}
	\label{fig:modelo_3D}
\end{figure}
\FloatBarrier

Durante la integración de esta arquitectura, surgieron dos problemas técnicos que requirieron desviaciones de la configuración por defecto para estabilizar la plataforma de simulación:
\begin{itemize}
	\item \textbf{Incompatibilidad en el Middleware (DDS):} Inicialmente se presentaron cortes intermitentes en la comunicación y pérdida de paquetes entre plataformas. El problema se resolvió modificando el estándar de comunicación de red hacia \textit{Cyclone DDS}, lo que garantizó un intercambio de datos robusto y compatible entre MATLAB y ROS. Suele ser un problema muy común el la distribución \textit{Humble} que se empleó para el proyecto.
	
	\item \textbf{Sincronización del Solver de Simulink:} El motor de cálculo numérico de Simulink tendía a desfasarse de la frecuencia de actualización que exige ROS para la publicación de sus tópicos. Para solucionarlo, se configuró el \textit{solver} en un paso de tiempo fijo discreto.
\end{itemize}

Si se desea conocer en detalle la configuracíon de \textit{Simulink} así como la conversión de un vector numérico en un mensaje estándar de ROS, consulte el Anexo \ref{ap:conexion_ROS}. 

\subsection{Simulaciones Recursivas}

Para finalizar el bloque de simulación, se desarrolló una arquitectura alternativa basada en la programación estructurada de un bucle iterativo, prescindiendo de dependencias externas de entornos de simulación comerciales. El desarrollo de este entorno de simulación "en código" responde a dos objetivos fundamentales:

\begin{itemize}[leftmargin=1cm, topsep=1mm, itemsep=0.5mm]
	\item \textbf{Portabilidad:} El código fuente es reproducible y ejecutable en cualquier plataforma. Aunque se implementó inicialmente en MATLAB para mantener la integración con el resto del proyecto, esta estructura facilita su futura migración a nodos de ROS en lenguajes como C++ o Python.
	\item \textbf{Transición natural hacia la implementación real:} La estructura recursiva del código emula fielmente el bucle de control de un microcontrolador real (que trabaja a intervalos de tiempo fijos), simplificando el paso del software al hardware definitivo.
\end{itemize}

Para lograr esto de forma autónoma, se sustituyó el motor de física comercial por un algoritmo de integración numérica elemental (método de Euler de primer orden), los valores de los estados ($x_k = [x, \theta, \dot{x}, \dot{q}]^T$) en la siguiente iteración del bucle vienen dados por la expresión recursiva:
\begin{equation}
	\mathbf{x}_{k+1} = \mathbf{x}_k + T_s \cdot f(\mathbf{x}_k, u_k)
\end{equation}

Esta ecuación gobierna el comportamiento del simulador en cada paso de tiempo y se compone de los siguientes elementos:
\begin{itemize}[leftmargin=1cm, topsep=1mm, itemsep=0.5mm]
	\item $\mathbf{x}_{k+1}$: Representa el \textbf{estado futuro} del sistema (posiciones y velocidades que tendrá el mecanismo en el siguiente instante).
	\item $\mathbf{x}_k$: Es el \textbf{estado actual} conocido en la iteración presente.
	\item $T_s$: Es el \textbf{periodo de muestreo}, es decir, el pequeño intervalo de tiempo que transcurre entre cada iteración del bucle.
	\item $f(\mathbf{x}_k, u_k)$: Es la función que describe la \textbf{dinámica física del sistema}. Combina el estado actual $\mathbf{x}_k$ con la acción de control en ese momento ($u_k$) para devolver las velocidades y aceleraciones instantáneas del mecanismo.
\end{itemize}

Mientras que las velocidades se obtienen de forma directa, el cálculo de las aceleraciones dentro de la función $f(\mathbf{x}_k, u_k)$ requiere resolver algebraicamente las ecuaciones que definen la dinámica del sistema abordadas en la Sección \ref{cap:modelado_teoria}. El desarrollo matemático completo para resolver este sistema dinámico y su traducción algorítmica al código de simulación se detallan en el \textbf{Apéndice X}.
\subsubsection{Problemas Encontrados y Compromiso del Periodo de Muestreo}

El principal reto técnico durante el desarrollo de este entorno simulado fue la aparición de \textbf{inestabilidades numéricas artificiales}. El éxito de la simulación depende críticamente de la elección del periodo de muestreo ($T_s$). 

Debido a que el método de Euler asume de forma simplificada que las aceleraciones del sistema se mantienen constantes durante todo el intervalo $T_s$, se detectó que valores de tiempo excesivamente grandes entre iteraciones acumulaban un error de truncamiento que no correspondía con la realidad física. 

\begin{figure}[ht]
	\centering
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Pictures/sim_discreta_10ms} % Foto de la electrónica vieja
		\caption{Simulación con $T_s = 10 ms$}
	\end{subfigure}
	\hfill
	\begin{subfigure}{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{Pictures/sim_discreta_500ms} % Foto de la suciedad en el raíl
		\caption{Simulación con $T_s = 500 ms$}
	\end{subfigure}
	\caption{Dependencia de $T_s$ en la simulación con expresiones recursivas}
	\label{fig:Ts_dependencia}
\end{figure}

Para concluir, la Figura \ref{fig:Ts_dependencia} ilustra el impacto crítico del periodo de muestreo al aplicar el mismo control de posición por realimentación del estado. En ella se observa cómo un intervalo de $10\text{ ms}$ produce una respuesta estable y fiel al comportamiento real del sistema, mientras que elevar dicho periodo a $500\text{ ms}$ degrada por completo la convergencia numérica, provocando una respuesta oscilatoria e inestable que distorsiona la física del mecanismo. 
\newpage

\section{Sistema físico}

\subsection{Programación del microcontrolador}
1)Firmware, comentar como se programa una DSP de texas instruments mediante el uso del IDE CCS y la capa de HAL C2000-ware. Utilizar el datashet del fabricante para justificar la configuración de ciertos registros. Módulos Epwm y Eqep muy interesante ver como liberan a la CPU de estas tareas. Explicar la configuración de registros para conseguir cierto comportamiento en los periféricos así como explicar como se configuró el protocolo serie para la telemetría del sistema. 

2)Explicar como se programo la DSP con timers para ejecutar el bucle de control en una rutina de interrupción periódica (implica determinismo, nada del loop de Arduino).

\subsection{Problemas con la zona muerta}

3)Ajuste de la ZM del actuador, explicar técnicas que se han utilizado para reducirla y compensarla. Compensación de ZM y ajuste de la frecuencia de la PWM para obtener menor zona muerta y mayor linealidad. 

\subsection{Identificación de parámetros}
4)Razonar experimentos de identificación de parámetros del sistema. La justificación es que los parámetros del fabricante no sabemos si son del todo ciertos al ser una máquina de hace 25 años. Algunos experimentos: Conversión de voltios a fuerza horizontal (encontrar la constante que relaciona ambas magnitudes), Identificación del rozamiento horizontal del carro, identificación del rozamiento y la inercia del péndulo. 

5)Simular de nuevo con los parámetros obtenidos mediante identificación. 

\subsection{Algoritmo de control en el microcontrolador}
6)Implementación de algoritmos de control en el microcontrolador (lo tenemos todo dividido en funciones, explicar un poco que recibe y devuelve cada una y como se ajusta todo)


Para la configuración de los periféricos se ha optado por el uso de estructuras de campos de bits (Bit-fields) proporcionadas por la SDK \textit{C2000Ware}. A diferencia de la librería de abstracción \textit{DriverLib}, este enfoque permite un acceso directo y optimizado a los registros del procesador mediante el uso de punteros a estructuras mapeadas en memoria. Esta elección garantiza la máxima eficiencia en la ejecución, reduciendo la sobrecarga de funciones (\textit{overhead}) y facilitando el control preciso de registros críticos como los de los módulos \textit{eQEP} y \textit{ePWM}.

\chapter{Resultados}

\section{Resultados en simulación}
\section{Resultados en sistema físico}

\chapter{Conclusiones y posible trabajo futuro}

Trabajo a futuro --> Implementación en HAL, uso de algoritmos no basados en modelo (Reinformemnt Learning)


%---------------
%	PART
%---------------

%\part{Part One}


%----------------------------------------------------------------------------------------
%	BIBLIOGRAPHY
%----------------------------------------------------------------------------------------
%\bibliographystyle{apalike}


\chapter*{Bibliografía}
\addcontentsline{toc}{chapter}{\textcolor{ocre}{Bibliografía}}

\printbibliography[heading=bibempty]



%----------------------------------------------------------------------------------------
%	INDEX
%----------------------------------------------------------------------------------------

%\cleardoublepage
%\phantomsection
%\setlength{\columnsep}{0.75cm}
%\addcontentsline{toc}{chapter}{\textcolor{ocre}{Index}}
%\printindex

%----------------------------------------------------------------------------------------

\chapter{Apéndices}
\label{chap:apendices}

% --- CONFIGURACIÓN DE NOMBRES PARA LAS SECCIONES (A, B, C...) ---
\setcounter{section}{0} % Reinicia el contador de secciones
\renewcommand{\thesection}{\Alph{section}} % Hace que las secciones sean A, B, C...

% ==================================================================
% APÉNDICE A: Desarrollo matemático
% ==================================================================
\section{Desarrollo del Lagrangiano}
\label{anexo:desarrollo_dinamico}

En este anexo se detalla el desarrollo matemático paso a paso del formalismo de Euler-Lagrange, partiendo de las energías del sistema hasta obtener la formulación matricial.
\clearpage
% ==================================================================
% APÉNDICE B: Código de la Planta
% ==================================================================
\section{Dinámica en MATLAB Functions}
\label{ap:codigo_dinamica}

Este apéndice agrupa los bloques de código implementados en Simulink mediante estructuras \textit{MATLAB Function} para modelar la dinámica del sistema mecánico de dos grados de libertad.

% ------------------------------------------------------------------
% Función 1: M(q)^-1
% ------------------------------------------------------------------
\begin{matlabbox}{MATLAB Function $M(q)^{-1}$}
	function Acel = Minv(q,par, J_p, M, m, l)
		M = [M + m,          +m*l*cos(q(2));
		     +m*l*cos(q(2)),      J_p];
		Acel = M\par; %Division de matrices.
	end
\end{matlabbox}
Esta función calcula el vector de aceleraciones del sistema a partir de la inversión numérica de la matriz de inercia. Para optimizar el coste computacional, evita el cálculo explícito de la matriz inversa utilizando el operador de división a la izquierda.

\begin{itemize}[leftmargin=1.5cm]
	\item[\textbf{Entradas:}] Vector de posiciones $q$, vector de fuerzas/pares generalizados remanentes $par$ (derivado de restarle a las fuerzas de control los efectos de fricción, gravedad y Coriolis), inercia del péndulo $J_p$, masa del carro $M$, masa del péndulo $m$, y distancia al centro de masas $l$.
	\item[\textbf{Salidas:}] Vector de aceleraciones generalizadas $\ddot{q} = [\ddot{x}, \ddot{\theta}]^T$.
	\item[\textbf{Operación:}] Resuelve algebraicamente el sistema lineal para obtener $\ddot{q} = M(q)^{-1} \cdot \tau_{neto}$.
\end{itemize}




% ------------------------------------------------------------------
% Función 2: C(q_dot, q)
% ------------------------------------------------------------------
\begin{matlabbox}{{MATLAB Function $C(\dot{q}, q)$}}
	function par_c = Cq(qdot, q, cx, ctheta, m, l)
		C = [cx,   -m*l*qdot(2)*sin(q(2))
		      0,          ctheta        ];
		par_c = C*qdot;
	end
\end{matlabbox}
Esta función evalúa los efectos dinámicos asociados a las fuerzas centrífugas, de Coriolis y a los coeficientes de amortiguamiento viscoso presentes en las articulaciones del mecanismo.

\begin{itemize}[leftmargin=1.5cm]
	\item[\textbf{Entradas:}] Vector de velocidades $qdot = [\dot{x}, \dot{\theta}]^T$, vector de posiciones $q = [x, \theta]^T$, coeficientes de fricción viscosa ($c_x, c_\theta$), masa $m$ y longitud del péndulo $l$.
	\item[\textbf{Salidas:}] Vector de fuerzas generalizadas de Coriolis y amortiguamiento $par\_c = C(\dot{q}, q)\dot{q}$.
	\item[\textbf{Operación:}] Construye la matriz no lineal $C(\dot{q}, q)$ y realiza la multiplicación matricial por el vector de velocidades ($\dot{q}$).
\end{itemize}
\newpage
% ------------------------------------------------------------------
% Función 3: G(q)
% ------------------------------------------------------------------
\begin{matlabbox}{{MATLAB Function $G(q)$}}
	function par_g = Gq(q, m, g, l)
		G = [0; m*g*l*sin(q(2))];
		par_g = G;
	end
\end{matlabbox}
Esta función determina el par generado por el campo gravitatorio sobre el péndulo basándose en su posición angular.

\begin{itemize}[leftmargin=1.5cm]
	\item[\textbf{Entradas:}] Vector de posiciones $q$, masa del péndulo $m$, constante de gravedad $g$, y longitud $l$.
	\item[\textbf{Salidas:}] Vector de fuerzas gravitacionales generalizadas $par\_g = G(q)$.
	\item[\textbf{Operación:}] Evalúa el par gravitatorio que afecta exclusivamente al segundo grado de libertad (coordenada angular del péndulo $\theta = q(2)$).
\end{itemize}

% ------------------------------------------------------------------
% Función 4: B
% ------------------------------------------------------------------
\begin{matlabbox}{{MATLAB Function $B$}}
	function tau_b = B(u)
		tau_b = [1; 0]*u;
	end
\end{matlabbox}
Esta función mapea la acción de control hacia el espacio de coordenadas generalizadas del modelo dinámico (matriz de acoplamiento de la entrada).

\begin{itemize}[leftmargin=1.5cm]
	\item[\textbf{Entradas:}] Señal de control escalar $u$ (fuerza aplicada sobre el carro).
	\item[\textbf{Salidas:}] Vector de fuerzas generalizadas de entrada $tau\_b = B \cdot u$.
	\item[\textbf{Operación:}] Proyecta la fuerza $u$ sobre el primer grado de libertad (movimiento lineal del carro), asumiendo que el péndulo no tiene ningún actuador.
\end{itemize}

\section{Código Control de Energía}
\label{ap:control_energia}

\begin{matlabbox}{MATLAB Function Swing-Up con PFL}
function F = fcn(q, qdot, E_ref, m, l, ctheta, cx, J_p, M)
	% --- PARAMETROS FISICOS Y GANANCIAS ---
	g = 9.81;
	Ke = 26;
	kp = 6;
	kd = 4;
	% -- ESTADOS DEL SISTEMA --
	x = q(1);        % Posicion del carro
	theta = q(2);    % Angulo del pendulo
	xdot = qdot(1);  % Velocidad del carro
	thetadot = qdot(2); % Velocidad angular
	% --- 1. CALCULO DE ENERGÍA ---
	Ep = m * g * l * cos(theta);
	Ek = 0.5 * J_p * thetadot^2;
	E_total = Ep + Ek;
	% --- 2. LEY DE CONTROL DE ENERGIA (Aceleracion 'u') ---
	e = E_total - E_ref;
	u = Ke * thetadot * cos(theta) * e - kp * x - kd * xdot;
	% --- 3. LINEARIZACION POR RETROALIMENTACION PARCIAL (PFL) ---
	% Convierte la aceleracion deseada 'u' en Fuerza 'F' para el motor
	thetadotdot = 1/J *(-m*l*cos(theta)*u ...
	+ m*g*l*sin(theta) -ctheta*thetadot);
	F = (M + m) * u ...
	- (m * l * thetadot^2 * sin(theta)) ...
	+ (m * l * cos(theta)) * thetadotdot ...
	+cc*xdot;
	% --- 4. SATURACION --- 
	F_max = 2; % Newtons
	if F > F_max
	F = F_max;
	elseif F < -F_max
	F = -F_max;
	end
end
\end{matlabbox}
\noindent

\begin{itemize}[leftmargin=1.5cm, topsep=1mm, itemsep=0mm, parsep=0mm]
	\item[\textbf{Entradas:}] Vector de posiciones $q = [x, \theta]^T$, vector de velocidades $qdot = [\dot{x}, \dot{\theta}]^T$, energía mecánica de referencia del péndulo en posición vertical $E_{ref}$.
	\item[\textbf{Salidas:}] Fuerza de empuje $F$ (en Newtons).
	\item[\textbf{Operación:}] El algoritmo ejecuta consecutivamente cuatro etapas fundamentales:
	\begin{enumerate}[leftmargin=0.5cm, topsep=1mm, itemsep=0mm]
		\item \textbf{Cálculo de Energía:} Evalúa la energía mecánica total instantánea del péndulo ($E_{total} = E_k + E_p$).
		
		\item \textbf{Control de Energía:} Genera una aceleración de referencia $u$ proporcional al error de energía ($e$).
		
		\item \textbf{Linealización PFL:} Traduce analíticamente la aceleración virtual $u$ en la fuerza real $F$ requerida para compensar la dinámica no lineal del carro.
		
		\item \textbf{Saturación:} Limita la salida a un umbral simétrico ($\pm 2\text{ N}$).
	\end{enumerate}
\end{itemize}

\section{Configuración Conexión MATLAB ROS}
\label{ap:conexion_ROS}

\begin{matlabbox}{MATLAB Function conversión a mensaje de ROS}
function msg = create_msg(blankMsg, q, time, start_time_epoch)
	msg = blankMsg;
	%Sincronizacion con el reloj interno de la máquina.
	total_time = start_time_epoch + time;.
	s = floor(total_time);
	ns = (total_time - s) * 1e9; 
	msg.header.stamp.sec = int32(s);
	msg.header.stamp.nanosec = uint32(ns);
	% 1. Configurar NOMBRES (coinciden con URDF)
	strArrayToAssign = {'cart_slider', 'pole_joint'};
	positionsToAssign = [q(1), q(2)];
	numStringsInArray = length(strArrayToAssign);
	msg.name_SL_Info.CurrentLength = uint32(numStringsInArray);
	for idx=1:numStringsInArray
		str = strArrayToAssign{idx};
		strLength = length(str);
		msg.name(idx).data(1:strLength) = uint8(str);
		msg.name(idx).data_SL_Info.CurrentLength = uint32(strLength);
	end
	% 2. POSICIONES (float64 array)
	numPositions = length(positionsToAssign);
	msg.position_SL_Info.CurrentLength = uint32(numPositions);
	msg.position(1:numPositions) = positionsToAssign;
	% 3. VELOCIDAD y ESFUERZO (vacíos)
	msg.velocity_SL_Info.CurrentLength = uint32(0);
	msg.effort_SL_Info.CurrentLength = uint32(0);
end
\end{matlabbox}

Esta función se encarga de mapear la información matemática simulada hacia un mensaje de tipo \textit{sensor\_msgs/JointState}, permitiendo que el visualizador gráfico de ROS (RViz) interprete la telemetría en tiempo real de acuerdo a las articulaciones definidas en el archivo URDF del robot.

\begin{itemize}[leftmargin=1.5cm, topsep=1mm, itemsep=0mm, parsep=0mm]
	\item[\textbf{Entradas:}] Estructura base vacía del mensaje de ROS (\textit{blankMsg}), vector de posiciones generalizadas $q = [x, \theta]^T$, tiempo acumulado de la simulación (\textit{time}) y la marca de tiempo absoluta en formato Unix Epoch obtenida al iniciar el proceso (\textit{start\_time\_epoch}).
	\item[\textbf{Salidas:}] Mensaje estructurado \textit{msg} (\textit{sensor\_msgs/JointState}) listo para ser publicado en ROS.
	\item[\textbf{Operación:}] El algoritmo procesa los datos siguiendo el protocolo estricto de mensajería de ROS:
	\begin{enumerate}[leftmargin=0.5cm, topsep=1mm, itemsep=0mm]
		\item \textbf{Sincronización Temporal:} Combina el tiempo base Unix con el reloj local del solucionador de Simulink, descomponiendo el tiempo total en segundos enteros (\textit{sec}) y nanosegundos fraccionarios (\textit{nanosec}) para rellenar el campo \textit{header.stamp}.
		
		\item \textbf{Asignación de Identificadores (Names):} Configura un arreglo de caracteres con los nombres de las articulaciones virtuales descritas en el URDF (\textit{'cart\_slider'} y \textit{'pole\_joint'}). 
		
		\item \textbf{Inyección de Telemetría (Positions):} Almacena de forma indexada el vector de estados continuos $q$ dentro del arreglo de tipo \textit{float64} reservado para las posiciones.
		
		\item \textbf{Vaciado de Campos Auxiliares:} Configura explícitamente a cero la longitud de los arreglos de velocidad y esfuerzo (\textit{velocity} y \textit{effort}), solo se requiere el envío de datos posicionales para la sincronización de la cinemática directa en RViz.
	\end{enumerate}
\end{itemize}

En la Figura~\ref{fig:solver} se detalla la parametrización del \textit{solver} numérico. Se ha seleccionado un paso de tiempo fijo (\textit{Fixed-step}) con el algoritmo de integración \textit{ode14x}, estableciendo un tamaño de paso de $\Delta t = 0.01\text{ s}$ ($10\text{ ms}$). 

\begin{figure}[ht]
	\centering
	\includegraphics[width=1.0\textwidth]{Pictures/solver}
	\caption{Configuración del \textit{solver} con paso de tiempo fijo}
	\label{fig:solver}
\end{figure}
\FloatBarrier

Por otro lado, como se ilustra en la Figura~\ref{fig:hw_impl}, dentro del menú \textit{Hardware Implementation} se ha establecido como objetivo la plataforma \textit{Robot Operating System 2 (ROS 2)}. 

\begin{figure}[ht]
	\centering
	\includegraphics[width=1.0\textwidth]{Pictures/hardware_implementation}
	\caption{\textit{Hardware Implementation}}
	\label{fig:hw_impl}
\end{figure}
\end{document}
